Meeting Agenda \- Sep 4th, 2026
Agenda:

* Updates from Nhat
  * Nhat: I’m working on the splicing, on the case when it resolves to a primitive type, and the surrounding diagnostics
  * Aaron: are you getting enough reviews?
  * Nhat: i have some open PRs, and would like another round of reviews.
  * Shafik: links?
  * [https://github.com/llvm/llvm-project/pull/190356](https://github.com/llvm/llvm-project/pull/190356)

* Shafik: my concerns is that with expansion statements, it’s hard to test them thoroughly without implementing reflection
* Corentin: why do you think it’s needed?
* Shafik: many use cases involve reflection
* Ambrose: I think the main issue with the feature is that it breaks long-held assumptions in our implementation. IF you’re in a function, you have a declcontext.
* Aaron: what is the declcontext for expansion statements?
* Ambrose: it has its own declcontext. Expansion statements need to be treated as a template. If you have a local variable in a body of an expansion statement, it’s declcnotext is that of the expansion statement, and not of a function. A few weeks ago I had LLM to run through Sema and parser to find all the issues, and it found like 50 things after I checked them on the godbolt.\\
* Vlad: this reminds me of structured bindings can introduce a pack, because it was also trying to get implicit template regions in normal code. We’re preparing the codebase for that by implementing expansion statements
* Ambrose: yes, and you can do similar stuff with expansion statements that that paper wanted to do
* \[. . .\]
* Corentin: I wish we had a differet design where dependency is not tied to declcontext, but it might be too late for that
* Aaron: I’m definitely worry about declcontext of a statement is not a function. But I’m also concerned that expansion statement is a statement, not a declcontext
* Ambrose: there are expantion statement decks with expansion statement inside
* Aaron: long-term we might end up with an unmaintainable codebase if those features keep piling up
* Corentin: I was not happy with the design Ambrose came up with, but I admit it was the only option we had
* Ambrose: yeah, no one is happy about it being a decl, but otherwise we’d need to refactor declcontext
* Corentin: the context of a requires clause needs to be skipped, which is one of the declcontexts we need to skip, not just expansion statement. All the places that look at the parent context are probably a bug
* Ambrose: yes
* Aaron: I’m concerned we’re putting hacks instead of refactoring. We can’t do this forever. Reflection is going to take a lot of time, so why do we need to do expansion statements now, with all the hacks?
* Corentin: expansion statements are useful on their own: tuple visitation, etc. No need to conflate the two
* Aaron: if we know the refactoring is coming, why not do it know
* Corentin: issues with lambdas are linked to this, actually, but for the past 15 years no one found a way to address them. It should not be a reason to not implement features
* Aaron: I don’t completely disagree, but WG21 is going to pile up on that. Aren’t we headed to a place when we can’t support C++ anymore, until we take a decade to refactor.
* Corentin: no one is paid to do that
* Aaron: people are working on this already. If we can’t bear the costs, we need to notify WG21. We kind of did with that paper
* Shafik: I think what Aaron is saying that we’re heading into the ball of mud state which is not sustainable. He doesn’t take the stance of being belligerent in WG21. I suggest that we have CFE internals manual, and we should be filling it. We also need to open issues to keep track. Currently this is in our heads, but it’s the wrong place.
* Vlad: Shafik’s suggestion to document and file issues; I agree, but how good will we be in the future about keeping that documentation up to date. Maybe comments in the code are easier to maintain? I’m concerned that with the timeframes we’ve seen in the past, this kind of documentation can get out of date. As for filing issues, we can try, but are you convinced our bug tracker is in the position for that? It still feels like thousands of issues we currently have need a lot of love to make our bug tracker easier to use. To Aaron’s point, when do we need to pay the technical debt – we know there are some people who are looking at adopting Clang for their implementations. It would be unfortunate if we declare we’re going into maintenance mode before those folks can bring additional resources.
* Shafik: on issues, I think that if we combine a GitHub project for technical debt issues, that would help. We track regressions similarly and that’s worked pretty well. It’s not perfect, but it might be acceptable.
* Vlad: glad to hear that the regressions project is working well, that seems like a reasonable idea for tech debt too
* Aaron: I’m not aiming at belligerence, sorry if it came out this way. If there is enough push, enough resources. I’m afraid we’ll  be stuck with good enough and build on top of it. I hope we can get maintainability good enough before implementing features. I don’t see the pressure to implement them. Those are my concerns as a lead maintainer
* Shafik: documentation forces us to have a discussion, and understand the scope better. Maybe after discussion we can come with better solutions or realize that the refactoring is not as bad. Or maybe it’s really a hard problem.
* Aaron: the thing I like about documentation suggestion is that everything is still fresh in our minds. Couple of years down the line, and we’d need to rediscover a lot of context.
* Corentin: there is nothing on fire. Even if declcontext is used for more than declcontext, maybe it’s fine. The language makes us to abandon certain assumptions. We should fix declcontext, but it’s not that bad. Maybe we need a separate notion of template region. I don’t think we’re accruing horrible technical debt here, compared to lambdas or aliases. If one day we decide we need to fix the tech debt, and introduce the template region, expansion statement will not be the hard problem to solve there.
* Aaron: my understanding is that we don’t need to use declcontext at all here. The notion that we need to go from a statement to a function is not changed by the language.
* Corentin: it’s broken by requires clause
* Aaron: I need to see the code
* Corentin: …
* Aaron: I think we’re talking about different assumptions. I thought that whenever you have a statement, it goes to function decl context, and constraint is a function decl context
* Corentin: sometimes we need to skip the lambda closure object depending on where we are
* Aaron: but it doesn’t apply to statements, right?
* Corentin: maybe
* Ambrose: we don’t want to attach dependent things to non-dependent context
* Aaron: does expansion statement make the function dependent? It sounds to me that we decided to implement expansions statement as if they are in a fucntion template
* Ambrose: the language requires us to instantiate it. The body of the expansion statement can have dependent things even if the initializer is not.
* Aaron: wow
* Aaron: are we asking for any changes here, in terms of documentation?
* Ambros: we have some in the form of comments in the implementation
* Shafik: I’d like to see more than that
* Ambrose: I can do that, but you need to tell me what I need to document
* Ambrose: I agree there is a preexisting issue. When you use a type and cast it, it’s always a bug, because you need a canonical type. Same for declcontext
* Aaron: you’re correct we want people to use named functions to navigate the declcontext hierarchy, but we have 20 years of people not needing that. We can catch all upstream usages, but I’m worried about downstreams
* Shafik: this is a kind of thing I want to see in the internals manual, because that’s been a long-standing assumptions. What made me concerned is the effort you went though to figure out things that need to be fixed.
* Ambrose: can we delete the overloads of cast and dyn\_cast on type and declcontext? Documentation might not be enough, we need enforcement.
* Hubert: I think I agree we should pursue such a change, and if we need migration for legitimate cases as well
* Ambrose: …
* Vlad: maybe we can insert assertions for the type being canonical in the cast path?
* Ambrose: we can allow it on CanQualType
* Aaron: you’re talking about free casts, right?
* Ambrose: Yes, member functions are not problematic.
* Aaron: there is a precedent for array types, but there is a gargantuan amount of code that casts type. It would be nice to hook up CastAs into casts. I’d like to avoid churn, because many uses are correct
* Ambrose: unless you attach attributes to a function, you’ll never crash because of this
* Aaron: there is code that looks at kind and then cast to that kind, and for those users it would be churn. CanQualType is there, but I’m not sure it’s used consistently. It would be nice if it was used more often, but it’s a lot of work
* Ambrose: we have sugar around things, and it depends whether we need that sugar. I’m not sure whether there is better way to model it. Other compilers I know of have the same issue of ignoring it ad-hoc
* Aaron: there is an academic response of pluggable type system, but java experience with that turned out to be not performant enough.
* Vlad: we’d need tests for refactor to make sure we don’t break the world.

Meeting Agenda \- Aug 21st, 2026

Agenda:

* Updates from Nhat
  * Reached out to GCC: Alignment of \`std::meta::info\` is intended to be pointer alignment. GCC notes alignment is important for the consteval-only values model.
  * Working on parsing for splice expressions:
    * Specifically splice-type-specifier because we only have reflections for primitive types right now.
    * No blockers: doing research
  * std::meta::info
    * Responded to Corentin; waiting for further discussion
  * Metafunction is\_type
    * Open PR (currently includes std::meta::info PR changes too; i.e., not a stacked PR)
* Hubert opened Core Issue on value representation of \`std::meta::info\`. Tentative direction is to have the value representation consist of zero bits. Lvalue-to-rvalue conversion to assume that the abstract value is the null reflection at run-time (i.e., outside of constant evaluation). https://cplusplus.github.io/CWG/issues/3217.html

Meeting Agenda \- Aug 7th, 2026

Agenda:

* Updated from Nhat
  * Corentin left a review on the [PR](https://github.com/llvm/llvm-project/pull/190356), I replied, need responses from Corentin
  * Waiting for this PR to finish before working on meta info
  * Corentin: PR has a per target size and alignment for meta info, and that worries me a bit because this type can appear at runtime. Why is this a per-target thing instead of it being the same size as void \*?
  * Ambrose: on x86 in GCC the size is 8 and the alignment is 1
  * Hubert: blech. Are they committed to that ABI now?
  * Ambrose: not sure, but that’s what they’re doing in practice
  * Hubert: the alignment being 1 is an odd choice. If they decided pointer size, why not just make it a pointer?
  * Aaron: did anyone reach out to ask if this was intentional?
  * Nhat: I don’t know who to reach out to
  * Corentin: Itanium ABI github might be the right place, but I’m not sure
  * Aaron: not mangling related, so maybe more psABI?
  * Corentin: not mangling
  * Corentin: odd that it can be configured independently of everything else
  * Ambrose: confirmed on godbolt, it’s the pointer size, not specifically 8\. On 32-bit targets, the size is 4, but alignment is still just one
  * Aaron, Valentyn: Jakub and Marek did the implementation work on GCC, they’d be good folks to reach out to
  * Steve: [https://godbolt.org/z/ez77djMf8](https://godbolt.org/z/ez77djMf8)
  * Aaron: does ABI compat with GCC matter here? You cannot mix and match Clang/GCC and the STLs in this case
  * Ambrose: you can run into an issue with a reflexpr and no include of the header anyway, the sizes could still be different because the type can leak to runtime.
  * Corentin: and the only value you can use at runtime is the null reflection, so it’s not helpful
  * Corentin: I’d prefer to simplify and not have a per-target size and alignment, but just go with one for all targets
  * Aaron: so not sizeof(void \*)?
  * Corentin: the current PR gets a different type with a different size, which is a bit different.
  * **Room: decision, we’ll go with sizeof(void \*) and alignof(void \*) for now.**
  * Aaron: we can always come back and change this decision because nothing is ABI stable for us currently.
  * Matthias: I think we should still reach out to GCC folks to ask why they did alignof(meta::info)==1
  * Room: agreed
  * Hubert: on the topic of runtime leak, have we decided how we want to handle initialization or the comparison?
  * Ambrose: for runtime?
  * Hubert: yes
  * Ambrose: there’s only one value?
  * Hubert: yes, but are all the bits formally padding bits?
  * Corentin: do we expect people to cast a meta info to int and expect it to be zero?
  * Ambrose: can you cast it at all?
  * Corentin: more about whether people expect a specific bit pattern. std::bit\_cast, lol
  * Ambrose: we could just zero it out, this really shouldn’t survive to codegen. As for comparison, it always is equal to itself.
  * Hubert: so it is all padding bits?
  * Ambrose: there’s no information that can be stored…
  * Corentin: when you do a bit\_cast and you are trying to convert padding bits, you get a warning.
  * Hubert: meta::info has constexpr unknown rep. So you can’t do it in a constexpr context.
  * Aaron: if it’s all padding bits, can it be merged via no\_unique\_address with other members?
  * Hubert: no. Similar to nullptr\_t, which I think we treat as if it has all padding bits
  * Aaron: okay
  * Hubert: bit\_cast, because we do have a builtin (and consteval in LLVM IR), if we do treat it as all padding bits, then you don’t get run tripping even at runtime (at least observable at some level of optimizations). int \-\> meta info \-\> int might not roundtrip to get the same int value back.
  * Corentin: do we want to ask GCC what they plan to do there? And do we need it to be compatible between the two?
  * Hubert: “why are you doing this?”
  * Ambrose: GCC currently doesn’t deal with this. Function of consteval only type must be consteval. So it doesn’t compile for them
  * Hubert: GCC might be right, I don’t think there’s a contextual conversion to bool.
  * Hubert: do we want to coordinate with GCC or do we want to talk to the committee about changing from constexpr only types to constexpr only values and whether that’s intended? We can ask them because for nullptr\_t, we changed the definition of lvalue to rvalue conversion so that it does not access to storage (not possible to race on it). We can ask if that’s supposed to be true for std::meta::info at runtime. I don’t know if that leads the committee to ask the same questions themselves.
  * Corentin: I think it would be nice for the standard to say something.
  * Hubert: at least that part is clear enough; I can ask about the race condition thing and see how much discussion that surfaces.
  * Corentin: can we ask about size and alignment too? We do it for nullptr\_t.
  * Hubert: I’ll ask in the same issue
  * Ambrose: I don’t think there’s a good reason to allow bit-casting to/from meta::info; the standard should just disallow it.
  * Hubert: if we end up with all padding bits, then we don’t need to add anything to the standard for that, but I guess the standard isn’t going to tell you it’s all padding bits. I’ll see what we say about nullptr\_t in that space.
  * Hubert: wording for nullptr\_ does not require all padding bits, it just says that’s possible in a note. Do we feel more strongly that we want to have more said here because of bit\_cast?
  * Corentin: I think it’s still worth asking the question
  * Steve: GCC behavior: [https://godbolt.org/z/e4f9ozoaK](https://godbolt.org/z/e4f9ozoaK)
  * Corentin: there’s possible interactions in the question with [https://www.open-std.org/jtc1/sc22/wg21/docs/papers/2026/p3969r0.html](https://www.open-std.org/jtc1/sc22/wg21/docs/papers/2026/p3969r0.html)
  * Corentin: calling bit\_cast with a meta info is never a constant expression, but it is still allowed at runtime for some reason?
  * Hubert: yes and yes.
  *
  *

Meeting Agenda \- Jul 24th, 2026

Agenda:

- [https://github.com/llvm/llvm-project/pull/211653](https://github.com/llvm/llvm-project/pull/211653)
  - Add support  for meta info to APValue
  - Should move header file to clang headers
  - Hubert: If we need standard library types, how do we run the tests ?
  - Ambrose: we can use small types / mock
  - Ambrose: There are too many interactions with the compiler so we need to vendor \<meta\>
  - Hubert: For C we vendor freestanding headers
  - Ambrose: we need to merge std::meta::info for primitive types first.
- Nhat to work on splice expression


Meeting Agenda \- Jul 10th, 2026

* Updates from Nhat
  * Nhat: Next step ?
  * Corentin: we can start to parallelize. Do we need table gen for builtins?
  * A: yes, there are a lot of builtins
  * Corentin: might look into consteval only values
  * A: I might look at define\_static\_string, etc
  * Nhat: I can look at table gen work for meta functions
  * A: That would make sense
  * Nhat: GCC is interpreting calls
  * A: are we gonna vendor the whole meta info header?
  * H: Yes, this is going to be on the compiler side, not the library.
  * Hubert: any ideas how to do the builtins/table gen work
  * Corentin: there needs to be some exploratory work
  * I'm concerned about std:: types
  * ( missed some messages)
  * Hubert: the builtins could fail on you, how are we gonna generate diags
  * Ambrose: do we need anything from vector beside data/size ? maybe we can call them
  * directly
  * Corentin : some vectors are input
  * Ambrose : so data \+ size
  * Hubert : the builtin can give you back an allocation the vector can copy
  * Corentin : it would be nice to have a vector ctr take ownership of an allocation
  * Hubert : at least we have the option to do that for libc++
  * Ambrose : the copy might be fine
  * Corentin : we can optimize that later
  * A: so the builtin returns an allocation you have to delete afterwards
  * A: you can call it twice... but if that computation is expensive, you do the work twice
  * H: and copying meta infos should be cheap
  * H: for this table gen, what's the scope. are we going to have a td file with a small same
  * A: There are a lot of meta::info \-\> bool or meta::info-\> meta::info functions. These should be
  * simple to implement.
  * H: Ok. Nhat, what do you want to be doing
  * N: Yes, I can work on that and try on some of these meta functions
  *
  * A: Planning to start merging expansion statement next week
  *
  * S: I doubt we have enough tests but this looks fine. I wish Aaaron could review.
  * A: I'll address and I'll merge into one PR
  * S: GCC has different behaviors
  * A: I believe Clang is correct
  * S: Great Just keep this is mind
  * A: Still trying to get code injection in 23

Meeting Agenda \- June 26th, 2026

* Updates from Nhat
  * Nhat is on vacation
  * He thinks that his [PR](https://github.com/llvm/llvm-project/pull/190356) is very close to being merged, so he’s asking what the next steps should be.
  * Vlad: I think the suggestion was to go deep instead of wide, once meta::info is ready
  * Corentin: yeah, the goal is to get to the point to share work between multiple people
  *
* [P4101R1](https://wiki.isocpp.org/uploads/2026-06_Brno-StrawPolls_-_P4101R1.html) “Consteval-only Values for C++26” was adopted in Brno as a defect report
  * The notion of consteval-only types was replaced with the notion of consteval-only values
  * Vlad’s understanding of the mental model is that implementations need to examine the values coming from the constant evaluator for the presence of non-null std::meta::info or a pointer to an immediate function.
  * Vlad: is it still the case that we’re just going to inspect the values coming out of the evaluator for non-null pointers coming out of immediate functions
  * Corentin: I think we want to flag when we see a meta::info?
  * Dan: I think you want to do it from check constant expression in the constant evaluator, and then some lexical stuff too like reflect expressions but that’s easier
  * Corentin: it’s not as bad as consteval only type
  * Dan: we might have to do a little bit of magic to handle the incomplete type stuff, but I haven’t looked at my notes
  * Ambrose: how does this work if you declare a variable that has a std::meta::info inside of it somewhere. That’s not a value.
  * Dan: not inherently ill-formed; the null reflection is allowed. You’d look at the initializer; if it’s a reflexpr then you can lexically determine that’s not allowed. If it’s an immediate function call then it basically comes out to a null reflection.
  * Ambrose: does meta::info have a default ctor?
  * Room: yes
  * Ambrose: so would we detect if that’s called at runtime?
  * Corentin: we’d just emit a null pointer for that. It’s weird, but…
  * Ambrose: what happens if you take a meta::info that is then returned but it’s not a consteval?
  * Dan: that’d be an immediate function
  * Corentin: I need to read the paper again.
  * Dan: I’ve not thought about it since Croydon
  * Vlad: so we are going to flag initializers in Sema, that they have something to do with consteval only values, and then in codegen we issue diagnostics?
  * Corentin: probably something like that
  * Dan: do we need to? You can tell whether it ends up being an info when checking constant expressions?
  * Vlad: but maybe that happens in a non-constexpr non-consteval variable?
  * Dan: but that’s not manifestly constant evaluated.
  * Aaron: delaying the check until codegen is not invalid, but makes it awkward for \-fsyntax-only users. Might be worth asserting in codegen instead
  * Corentin: I’m not looking forward to that, because we spent 4 years on not leaking consteval functions, and there are still bugs
  * Ambrose: we should be checking the reflection when we emit them
  * Corentin: we’d also need to deal with escalation
  * Aaron: I don’t insist
  * Corentin: I might work on it of no one else does, once the meta::info is there, because it’s the only consteval-only value
  * Vlad & Dan: pointers and pointer to members to immediate functions are consteval-only
  * Corentin: I think this is something someone has to implement to see whether it’s viable or not.
* Iteration statements (a decision was made during the last meeting)
  * Vlad: was it implemented? did it work?
  * Ambrose: it emits a diagnostic if you try to compile an iterating expansion statement
  * Corentin: I need to review that
  * Ambrose: I have experimented with the token injection approach but that’s a follow-up after the main work has landed.
  * Vlad: and we did this because was wanted it in 23
  * Ambrose: I think there were a few more comments but I don’t think there’s anything major left to work on. Ideally, I’d like to also get the follow-up patch for iterating expansion statements into 23 as well.
  * Corentin: that would be good, but review bandwidth might be tight for the timing
* Dan published the third attempt to add the interface to mutate AST during constant evaluation ([PR](https://github.com/llvm/llvm-project/pull/205557))
  * Does not target 23 release
  * Vlad: I’ve not looked at this yet
  * Ambrose: didn’t Corentin land something for this?
  * Corentin: I did try, but it didn’t land.
  * Dan: Richard asked for a whole-scale redesign of what Corentin did
  * Dan: I tried to implement this in line with what Richard asked for; basically, provide a proxy to sema to the evaluation functions.
  * Dan: I talked to Richard when it was halfway done to make sure it was what he was looking for and he confirmed (at a high level).
  * Dan: I have mixed feelings about the design now that it’s closer to done. Richard’s take is that he wants side effecting APIs to have a clear and visible signal that the API could be mutating the AST. I kind of like that as an idea. On the other hand, the only real signal at the call site is if you remember which evaluation functions are mutating and which ones are for values. So it increases API surface area and cognitive overhead; you can’t just search for something to find all the places where mutations are allowed. It also doesn’t have an obvious link to the wording specification.
  * Vlad: the checking part is concerning
  * Dan: yeah, but it is what it is, the standard should do a better job of saying that.
  * Ambrose: if a side effect happens during the evaluation and it turns out to not be a constant evaluation…
  * Dan: another idea to think about (it’s in a comment in the PR too): Corentin’s approach was, at the start of translation to put a proxy in the AST context and at the end, remove it from Sema. So there’s a scope as to when SemaProxy is available. The gate check on whether mutation is allowed is from within the constant evaluation. We could still put the sema proxy in the AST context so it’s accessible, but do it with an expression evaluation context. So you could push a new one, whenever pushing manifestly constant, that’s when you set the proxy in the AST context. Then the push and pop RAII is what does this rather than the evaluator having access through the entire TU.
  * Dan: I’d like to explore that design, I think it still has an explicit signal for when the evaluation is allowed to mutate or not. It seems like a reasonable compromise. But it does make it easier to search in the code where mutations are allowed and it ties to the wording better.
  * Vlad: how does this work with “inner” evaluations.
  * Dan: you would restore the SemaProxy to what it was when it was entered. You don’t clear it, you restore it.
  * Vlad: if you enter the evaluation with the context that permits mutation, when we do “inner” manifest constant evaluation, that should not be able to mutate the AST. GCC does not implement this.
  * Dan: if you push any other kind of evaluation context, it clears the sema proxy
  * Vlad: ah, if it works like that, let’s make sure we have good test coverage for the scenario to make sure it’s handled.
  * Corentin: did you talk to Richard about the design?
  * Dan: not yet, I’ll ping him about it sometime today.
  * Corentin: my PR had to do some things with trial evaluations; I don’t know if side effects should happen there or not.
  * Dan: do we use the constant evaluator in C?
  * Aaron: we do.
  * Dan: that may be a reason to keep this distinct
  * Corentin: if you have define\_aggregate during constant initialization, you have to deal with it?
  * Dan: No, only from a consteval block
  * Corentin: can’t you get there through a lambda?
  * Dan: if you evaluate the lambda during the evaluation, yes
  * Corentin: if the function is undefined we should not instantiate it because it may be instantiated later.
  * Dan: I checked explicitly whether the template it’s instantiated from has a definition before trying.
  * Aaron: I like the idea and it’s worth exploring. My concern is that it’s obvious at callsite, but it’s not obvious when you actually mutate.
  * Corentin: when you want to be explicit about mutation, why?
  * Aaron: invariants. I want to know when AST might mutate underneath me
  * Dan: those clear signals are for the purpose of review
  * Dan: the reviewer should be asking whether is the scope as narrow as it can be
  * Aaron: we’re talking about slightly different things. What If you add RAII scope, and sometime later mutate
  * Corentin: do we agree that mutation only adds nodes?
  * Vlad: no, redeclaration changes
  * Dan: also adding a definition to incomplete types
  * Aaron: in the future, will we mutate existing things?
  * Dan: I don’t think so
  * Aaron: I’ve heard that people want to add default arguments
  * Dan: I wish I will never hear “default arguments” again
  * Aaron: is there an API that copies an existing thing and drop something?
  * Corentin: that would be a new node
  * Corentin: members will be taken as individual reflections
  * Dan: I don’t have a horse here, but if token injection comes in, it will only introduce new stuff
  * Ambrose: are we’re going to have mutatable consteval variables?
  * Corentin: it is brought up in hallway every meeting
  * Vlad: what will be done to keep existing code working?
  * Dan: I don’t think we’ll ever be able to get away with that; I suggested to Barry we can later say an constexpr variable can escalate to consteval.
  * Corentin: I want to know whether this solution will be correct for Richard; if that doesn’t work, should we put everything that can mutate the AST in Sema, hiding everything at the AST level so the AST cannot be used to evaluate.
  * Dan: using the expression evaluation context would achieve that
  * Corentin: yes
  * Aaron: we won’t be able to hide everything in Sema, because users only have access to AST
  * Dan: but for clang-tidy, by that time the semantic analysis is done
  * Aaron: in any of the cases, we’d need to degrade gracefully
  * Corentin: sure, this is why I never understood this concern
  * Corentin: if someone will try to call define\_aggregate from lldb, it won’t work
  * Aaron: if people try to evaluate things later, it won’t work. We can’t get it worse than that in terms of API UX
  * Aaron: I don’t know if it meet Richard’s concerns, but I suspect it will
  * Dan: there are several methods in Sema that calls into AST, related to constant initialization. We might need to document things that can be called in manifestly constant-evaluated expressions
  * Aaron: if the segregation is that AST can never mutate anything, and you need something from Sema, I think this is the design we need
  * Corentin: I keep having the opposite concerns: what if we have places where mutation is supported, but we don’t do them.
  * Dan: in your model, the danger is that if something is sloppy, you’d allow mutation where you don’t want to. I think Richard and Aaron are correct that we’re better be conservative
  * Dan: I think the Clang sources shine when the structure reflects the standard wording
  * Corentin: can you explain the difference between what we call constant-evaluated and what the standard call manifestly constant-evaluated?
  * Dan: I don’t think there is, but I didn’t check. It still might be important because of C use cases
  * Ambrose: if something from C calls into Sema, something went horribly wrong
  * Corentin: if we’re in a switch statement, and we start a complicated ceremony to mutate, that won’t fly
  * Ambrose: we should never mutate anything in C mode
  * Vlad: Corentin had a concern of mutating from extensions that might be available in C
  * Ambrose: but then the reflection is still a C++ construct
  * Aaron: I’m thinking about blocks, which are basically lambdas. What if you have a define\_aggregate in it?
  * Corentin: meta::info is not a C thing
  * Aaron: but we have overloads in C
  * Aaron: C is looking into type trait-y things, but so long as define\_aggregate isn’t available, it’s fine
  * Dan: do we have a notion of how far do we want to backport it?
  * Corentin: as far as we could, because we have bugs, even beyond C++23 unique\_ptr
  * Aaron: do we need an ABI flag?
  * Ambrose: no
  * Dan: y’all seem to be tentatively happy with it. I’ll make sure Richard is happy with it
  * Vlad: we should invite Richard
  * Dan: I won’t be able to join on July 10th

Meeting Agenda \- May 29th, 2026

* Next meeting?
  * Happening in the middle of Brno. Next after that is Jun 26
  * Agenda is published and the meeting ends 30 minutes before this call
  * **Decision: cancel Jun 12**
* Updates from Nhat
  * Got another round of review from Ambrose
  * Will have an updated PR in the next few days
* Updates from CWG 2026-05-19 telecon
  * [P3125](https://www.open-std.org/jtc1/sc22/wg21/docs/papers/2026/p3125r5.html) Pointer tagging
    * The paper introduces an easy way to get invalid pointer values
    * First time to make it very easy to get invalid pointer values in the language, at least at constexpr time
    * Hubert: I wasn’t on the call, but I read the minutes. Was this the interface that people wanted to go away or make it optional?
    * Vlad: yes, that was the discussion. There were suggests to return uinptr\_t instead of a pointer type, etc. We will see what Hana comes back with.
    * Hubert: was there rationale for why it should return a pointer?
    * Vlad: I don’t recall any; Jens asked for a section in the prose to explain it
  * [CWG3166](https://cplusplus.github.io/CWG/issues/3166.html) “Protected access rule for a pointer-to-member from a reflect-expression”
    * It’s possible to reflect over deleted functions
    * Aaron: seems reasonable, at least within the design of reflection. Access control is basically ignored, and deleted functions are still functions with interesting properties like a name, parameters, etc that you could reflect over.
    * Hubert: so we dealt with deleted functions but not overload resolution, so yeah, makes sense that we’re allowing both.
    * Vlad: yeah, got rid of overload resolution and put in a specific rule for deleted functions (otherwise overload resolution fails if you select the deleted function but that’s not the behavior we need here)
    * Corentin: and there is a reflection query to test whether a function is deleted or not.
* Token injection for syntactic sugar
  * Relates to expansion statements. The problem is there are a few places where we need to generate a bunch of code in Sema that the user did not type: [https://eel.is/c++draft/stmt.expand\#5.2](https://eel.is/c++draft/stmt.expand#5.2)
  * We basically need to generate that blob of code in Sema. Making up a lambda out of thin air via calls to Sema is very involved and fragile.
  * Trying to come up with a solution that doesn’t involve ginning up AST nodes manually; better to come up with the code and have the compiler parse it like it usually would.
  * Corentin: the wording is using the lambda but can we get rid of the lambda all together?
  * Ambrose: how would you do that? You need to evaluate.
  * Vlad: we don’t know whether we need part of it until after we’ve done enough semantic analysis to determine we’re doing an iterating expansion statement. Trying to do that manually is hard.
  * Corentin: so the lambda is there just for an evaluation context?
  * Ambrose: yes, but it’s also a bit of a requirement because the constant expression evaluator needs the evaluation context (so we’d need a function or a lambda, etc to get the constexpr interpreter to work). Getting the details right via manual means is actually really hard; this suggestion is actually the easy approach.
  * Corentin: you only need a very small subset of the overall lambda code, not the whole set of interfaces.
  * Aaron: lambdas get changes with every release of C++, there’s a general maintenance burden to solve
  * Hubert: not generally opposed to generating text for parsing but my concern with such a mechanism are vague fears around this and name lookup. If there’s anything we could do to try to avoid actual (real) name lookup, that would be helpful.
  * Ambrose: there’s a plan for that; it lexes tokens in the string, then go through the identifier tokens that match these replacement tokens, we just replace (and avoid name lookup). So there’s an explicit mapping between identifiers and existing AST nodes.
  * Vlad: it may not be useful for Tom’s use case in SYCL, per the email chain we had with Ambrose, Aaron and I
  * Corentin: can we make progress on the other kinds of expansion statements before we land this? I’d like to avoid this for Clang 23 because we only have until July. The rest of expansion statements are in fairly good shape?
  * Vlad: we have a whole pile of PRs for this already?
  * Corentin: I’m suggesting we don’t implement iterating expansion statements
  * Ambrose: this functionality isn’t challenging to implement, we already have the logic for it for handling pragmas; so it’s a smaller amount of code than you might expect.
  * Vlad: what happens in the approach where you get the AST nodes directly in terms of diagnostics?
  * Ambrose: just like range-based for loops it points at the \`:\` token.
  * Vlad: I don’t think implementation is a concern; our library layering makes this easier. It’s more a question of what way we want to go.
  * Corentin: that’s why I’d like to hold off, there are alternative designs like a std::fmt-based.
  * Ambrose: this is effectively that approach, though not with the same syntax using braces.
  * Ambrose: there is a proxy object involved to avoid library layering violations though
  * Corentin: how do you make sure you don’t end up introducing identifiers that may change lookup results. e.g., we can have variables without a name when we make the AST nodes directly
  * Ambrose: it depends on what code you put in that string; if you declare random variables you’ll get them. But for things which refer to entities, we can use the placeholders to avoid the name lookup. So we currently don’t introduce names, but it is \*possible\* to do (just not a good idea to do so, as you point out)
  * Ambrose: macros are expanded though, you’ll see I’m using \_\_PTRDIFF\_TYPE\_\_ which needs to expand to something useful.
  * Aaron: that makes some interesting test cases for us where there’s a macro defined as \_\_begin.
  * Corentin: can we instead use the underlying type instead of a macro?
  * Ambrose: that’s probably also fine
  * Corentin: I’d prefer that to avoiding macro expansion
  * Ambrose: that should be plausible
  * Ambrose: are we fine with keeping the manual AST nodes for now and then switch to tokens later? Or do we still want to make iterating expansion statements an error?
  * Corentin: either is fine by me
  * Vlad: I’m fine with the plan to gin up AST nodes for Clang 23 as long as it’s followed-up that does token injection. Otherwise I’d rather leave iterating expansion statements as unimplemented.
  * Aaron: slight preference for unimplemented expansion statements
  * Corentin: remember Brno is happening so we don’t have as much time as you think
  * Aaron: good point\! I don’t insist.
  * Vlad: it would be nice to have discussion of this in a single place for everyone to participate.
  * Aaron: I can hold my nose and we can move forward with ginned up AST nodes if Ambrose is fine with that.
  * Room decision: move forward with ginned up AST nodes as in the PR, Ambrose makes the final call (if there are problems he can suggest alternatives)
  * For Clang 23 we’re not going to claim it’s implemented anyway (not setting the feature test macro), so we still have some wiggle room if there are bugs.
  * Ambrose: the current approach requires the ParseScope refactoring which moves into Sema. So we’d then move it to Sema and move it back out.
  * Corentin: oof, I really don’t want the parsing scope to be in sema…
  * Ambrose: yeah, and that’s required for the iterating expansion statements
  * Corentin: maybe just remove support for iterating expansion statements?
  * Ambrose: yeah, I think that makes more sense at this point.
  * **Room decision, this time for real**: remove support for iterating expansion statements for Clang 23 (we can figure out if it is iterating or not, but if it is iterating, it’s an error)


Meeting Agenda \- May 15th, 2026

* Update from Ambrose
  * Getting reviews on the expansion statements PRs, about half-reviewed. Thank you to Corentin for the review work\!
* Updates from Nhat
  * No major updates, still need reviews on the PR particularly from core Clang maintainers
  * Shafik said he’ll try to get to it soon
* Follow-up on [P3816R3](https://www.open-std.org/jtc1/sc22/wg21/docs/papers/2026/p3816r3.html) “Hashing meta::info”
  * R3 contains a new section 6.1.4 with implementer feedback, including ours from the last meeting.
  * R3 also contains section 9.2 about opt-in hash randomization following Corentin’s suggestion during the last meeting.
  * Valentyn: talked to GCC folks, they’d like to reuse the same implementation work for type comparison and for hashing meta info. Seems like everyone is on board, but it was a very short response
* [P4167R0](https://www.open-std.org/jtc1/sc22/wg21/docs/papers/2026/p4167r0.html) “reflection annotations to control ADL”
  * C++26 got syntax for annotations, non-ignorable, must be parsed but it was just syntax. This is the first paper trying to standardize an actual annotation. Similar situation to attribute syntax and standard attributes.
  * Vlad: I wonder how this will work with older compilers. It’s not ignorable, so I suspect it’ll have to be hidden behind macros.
  * Corentin: it’s just a syntax error in older compilers and language modes because of the equals sign.
  * Aaron: if we don’t know the annotation, that’s an error?
  * Corentin: yes, it involves lookup so the lookup would fail.
  * Corentin: my issue with the paper is that it requires the library to define the annotation. Using a keyword instead of an annotation seemed like a better approach.
  * Hubert: is that an implementation point of view? Are we fine with annotations having special types? If we have to evaluate it and then use it in semantic analysis?
  * Hubert: why can’t a user use things of these type other than annotations, they’ll need to smuggle it? From an evolution point of view, that worries me.
  * Corentin: this requires including the \<meta\> header for ADL to work which I also don’t like. But this seems like the start of using reflection everywhere.
  * Vlad: they’ll still have to hide it behind a macro in practice
  * Corentin: you don’t want it to compile in older language modes; you don’t want ADL to change behavior based on language mode
  * Vlad: so we’ll have C++-29-only libraries
  * Corentin: yeah, that’s basically part of the goal. If it was a keyword in the language, it would be the same issue though (don’t support the keyword is the same as the library not supporting the annotation), so that’s fine. I just don’t like the ergonomics of having to use the standard library to control ADL behavior.
  * Hubert: as a library-based solution, I have this feeling that I’m not entirely sure that the type with the special semantics is something the library can provide if the compiler doesn’t support it.
  * Corentin: you need to have the compiler and the library working in tandem
  * Hubert: but that’s the whole point; if the library has the type and is ready for it, but the compiler isn’t ready to use it…
  * Corentin: feature test macros could help; needs to be two, one for the language and one for the library.
  * Hubert: there’s a language one there, not a library one currently
  * Shafik: this means the library has another combination of things to do
  * Corentin: isn’t that all of reflection?
  * Shafik: I guess if the library folks are okay with it, then okay, but it seems like a combinatorial testing issue and I thought folks usually push back on this kind of design but I don’t know.
  * Shafik: what is the compile time hit from this?
  * Corentin: possibly improve compile times because of less lookup.
  * Shafik: but the hit was just due to size alone; combinatorics effects that. But I don’t know for sure.
  * Vlad: local variables can now become an associated namespace because it builds off associated expressions
  * Corentin: a variable shouldn’t though?
  * Vlad: the point of the feature is to add them; we will have to restrict which meta::info you can pass to this thing.
  * Corentin: maybe
  * Vlad: if you pass a reflection of a type alias, will it be dealiased? The wording ignores type aliases
  * Corentin: that’s how the existing wording works too, right?
  * Vlad: it does care about aliases, so it definitely needs some more work.
  * Corentin: what owns the span for \_data in the additional\_associated\_entities structure? That should be a vector? Nothing seems to own the memory.
  *
* [P4147R0](https://www.open-std.org/jtc1/sc22/wg21/docs/papers/2026/p4147r0.html) “constexpr \=\> runtime bridge”
  * Aaron: what is even going on? WG21 decided threads need to be constexpr so then it needs mutexes to be constexpr so that caused problems and now we need new special magic? This seems so wholly unmotivated, but I feel like I must be missing something.
  * Corentin: how does this work for trial evaluations?
  * Vlad: seems to be basically a callback to allow transforming compile-time buffers to runtime buffers via things like define\_static\_static.
  * Shafik: my objection is that we’re turning a constant expression engine into a whole operating system? What does this complexity buy users?
  * Aaron: do we have feedback to give WG21 beyond “please don’t”?
  * Corentin: I do have implementation concerns; a callback we have to call sometimes but not other times increases the implementation and maintenance problems for us. I did try to push back in LEWG and the responses are “by making everything constexpr then the compiler can constant fold better” and that requires the full library to be constexpr.
  * Hubert: trying to ask the compiler to constant fold (or just making it possible), it’s not always the case that constant folding everything is what the user wants or that it’s even the most efficient way to do the computation. That’s a questionable goal for LEWG to have.
* [P3377R0](https://www.open-std.org/jtc1/sc22/wg21/docs/papers/2026/p3377r0.html) “constexpr reinterpret\_cast partial replacements”
  * Corentin: this is doing constant evaluation assuming memory exists when it doesn’t, I’m not certain it can be implemented in the bytecode interpreter
  * Vlad: I think you have to have one in order to implement C++29 now, right?
  * Corentin: I don’t know that we track enough information to implement it, but tracking that adds overhead to every constant evaluation
  * Vlad: I assume we track pointer provenance, so casting to enclosed type seems implementatable
  * Corentin: maybe? Object cast is the one I’m more worried about
  * Vlad: is start\_lifetime\_as constexpr, that seems like it’s in the same space?
  * Hubert: FWIW launder is constexpr but only evaluates if you get back exactly what you gave it in the first place.
  * Corentin: APValue of an array trying to change to an APValue of a structural type with the same byte layout is the kind of situation I worry about. bit\_cast works because we transform the value and get something new back, but object\_cast is in-place where you get the same thing back. I don’t know how to make that work.
  * Vlad: recurring theme in today's meeting; how much do we have to emulate actual storage as opposed to tracking just values.
  * Corentin: as soon as you have memory regions, it becomes really hard to diagnose UB. Because we don’t actually have memory, it’s easy to catch UB.
  * Shafik: once you have arbitrary objects at arbitrary locations, you can do bit fiddling and navigate between things. So you need something more like Asan/Msan, which adds expensive overhead.
  * Corentin: if you modify random memory you also cannot check class invariants; you could alter the capacity of a vector by modifying the bytes, which introduces UB that we couldn’t catch.
  * Corentin: I hope we don’t have memory at constexpr time ever. This heads in the wrong direction.
  * Vlad: yeah the paper specifically wants bytes for storage so that you can reinterpret\_cast into subobjects/enclosing objects.
  * Vlad: do we have implementer objections then?
  * Corentin: we should get Timm’s opinion
  * Vlad: I’ll contact him to see where it goes.
* More consteval papers:
  * [P3852R1](https://www.open-std.org/jtc1/sc22/wg21/docs/papers/2026/p3852r1.html) “constexpr is\_pointer\_within memory area”
  * [P3818R2](https://www.open-std.org/jtc1/sc22/wg21/docs/papers/2026/p3818r2.html) “constexpr exception library which is unsurprising in potentially constant initialization”
  * [P3125R5](https://www.open-std.org/jtc1/sc22/wg21/docs/papers/2026/p3125r5.html) “constexpr pointer tagging”
    * Hubert: Might have issues on some targets where you cannot actually do this (IBM i comes to mind)

Meeting Agenda \- May 1st, 2026

* Updates from Nhat
  * Not present today. No updates, needs more reviews.
* [P3816R2](https://www.open-std.org/jtc1/sc22/wg21/docs/papers/2026/p3816r2.html) “Hashing meta::info”
  * [Croydon minutes](https://wiki.isocpp.org/2026-03_Croydon:EvolutionWorkingGroup:P3816R2) (require access)
  * Summary from Valentyn about the EWG discussion. Big open question is: do we want unstable, stable, or semi-stable hashing for meta::info?
  * Clang currently only implements semi-stable, but the atmosphere in EWG was that they want it as stable as possible. Will be discussed in SG7 next.
  * Aaron: I think semi-stable is the best we can do because we’re going to hash the mangling most likely. But not everything has manglings defined and the hashes will be different depending on which mangler is used (Itanium vs Microsoft). Those may not be actual problems, but worth keeping in mind.
  * Valentyn: that’s roughly the same as what GCC is expecting to do; we’re looking for implementer feedback on what kinds of issues we might have, if any.
  * Vlad: there was a numbering approach discussed last time.
  * Valentyn: that might work but the mangling approach may be less experimental; the numbering approach was just for the initial effort to see how it works
  * Matt: a bit worrying to try to have a fully stable hashing because ABI breaks are already sufficiently painful, so semi-stable may be sufficient.
  * Vlad: different versions of the same compiler, sure.
  * Vlad: I think trying to number things won’t work; we would number types and functions today. Say we don’t do partial specializations (not representable today). When we make them representable, newer compiler versions will also have to index those, so the numbering will change. Hashing of mangling is more viable in that scenario. But we also don’t know when the mangling definition will become stable.
  * Corentin: in 26 it is mandated for the ID to be stable across TUs. Hashing is not more restrictive than that. We’ve never tried to have IDs stable across implementations, I don’t think hashing needs to be either. So I’m not concerned.
  * Vlad: both Clang and GCC do as few ABI breaks as possible between versions. For the use cases we have for the hash, would it be acceptable for implementations to break ABI between compiler versions?
  * Valentyn: from what I understand, that’s acceptable. This is more for reproducible builds and caching.
  * Matt: yeah, we think it is acceptable. If you’re using it, it has to be within a consteval function. The ABI issues only come in really with code in header files using hash types. But that’s kind of a strange use case.
  * Aaron: my experience is that when you make ABI guarantees, users rely on it. Every abi issue is a problem. I wish we should be able to break ABI, but we should be prepared to treat it as a windows that closes quickly.
  * Corentin: again, in 26 people are going to rely on ABI for meta info in template info and it’s stable across TUs. We’re going to have the problem within the 26 timeframe. When you have the mangled name in your symbol name, whether you have a hash or the mangled name doesn’t matter, it’s a mess either way.
  * Aaron: I don’t disagree, except for the part when we decide when ABI breaks.
  * Hubert: You can currently smuggle properties from reflection space out of it, but with this hash situation and the ABI break discussion, it feels like this is slightly different from the usual mode of ABI break. Usually, you get into linkage difficulties (maybe wide enough to be “ODR”). In this case it’s similar, but it’s the data value that changes. You can avoid the ODR violation but your program may still behave differently. For us, we call that an ABI break. But for non-language/compiler folks, this mode of failure doesn’t seem to be the same thing.
  * Corentin: you can put the hash value in a template argument
  * Hubert: yes, but the mode of failure there is a link error because of the mismatch. But if you have the hash value stored somewhere that’s not part of the type, you’d get different output but that’s not a typical ABI break.
  * Vlad: agreed; leaking this hash into the runtime and branching on whether it’s even/odd, etc.
  * Matt: when we say we want the hash to be stable; as a user I just want to be able to use an unordered\_set and it be stable. The implementation of it can be implementation-defined and it seems fine that you get different behaviors in different compilers. Why would we want something different?
  * Vlad: reproducible builds; but those can require a user to opt in to a more expensive operation.
  * Aaron: is it enough for hashing to be implementation-defined? Yes. I think we’ll wind up hashing up the mangling, even if microsoft does something different. On GCC side it would be nice to be compatible with them, but I’m not sure if it’s even a requirement, because our implementation are different.
  * Corentin: there may be folks who want the opposite of stability because they want to test that people do not rely on things like hash values. So as an implementation, we may want to have an option to salt the hashes.
  * Aaron: precedent for this with \_\_DATE\_\_ and \_\_TIME\_\_ values; we’re not reproducible by default, but we give feature flags to let the user get reproducible build behavior. We might want a different default behavior here, but we can do this stuff.

Meeting Agenda \- Apr 17rd, 2026

* Updates from Ambrose
  * I'm working on constexpr exceptions (and as ever more reviewers for the expansion statements patches are always nice; Shafik reviewed the first patch yesterday)
  * Starting from Hana’s patch, needs to update a bunch of APIs to return a result as opposed to a bool; work ongoing
  * Hubert: consteval blocks, I saw a patch flying by; is someone tracking that?
  * Shafik: I reviewed it but we need more eyes on it. I didn’t find anything shockingly bad, but it definitely needs more eyes.
  * Vlad: if expansion statements are not going to land in the next few weeks, I’ll likely find time to go over core issue tests in there to ensure his behavior matches, but can’t do it currently due to timing.
  * Shafik: consteval blocks has no testing for ODR violations but there’s a lot of new diagnostics for them.
  * Vlad: did he take tests from Dan’s fork?
  * Shafik: for consteval blocks? I don’t know, that’s good to ask on the PR ([https://github.com/llvm/llvm-project/pull/188665](https://github.com/llvm/llvm-project/pull/188665)).
* Updates from Nhat
  * PR is opened and got some reviews, looking for more review ([https://github.com/llvm/llvm-project/pull/190356](https://github.com/llvm/llvm-project/pull/190356))
  * This patch adds support for mangling following this behavior [https://github.com/itanium-cxx-abi/cxx-abi/issues/208](https://github.com/itanium-cxx-abi/cxx-abi/issues/208)
  * Vlad: I have a code example to see if others are aware of the behavior:
    struct S;
    consteval void f() {
      const bool a \= is\_complete\_type(^^S);
    }
* When you have an initializer for a constant variable of integral type, it’s also manifestly constant evaluated. I totally forgot.
* Shafik: yeah it is kind of surprising when you see it for the first time. This comes back to C++11 when you could use constant-initialized variables in constexpr contexts. This is more of the same.
* Vlad: yeah, we do allow const int to be an array extent without becoming a VLA as an extension in C++
* Vlad: this is more of a reminder in case folks forgot, but no question about it.

Meeting Agenda \- Apr 3rd, 2026
(Meeting attendance was light due to the WG21 meetings last week, so this meeting was more informal.)

* Updates from Ambrose
  * Aaron: Ambrose didn’t make it today due to scheduling conflict. Erich approved all the patches for expansion statements, Eli is looking at codegen, looking for someone else for another round. He also has a patch for consteval blocks. After this is done, he’s willing to help with reflection.
  * Vlad: a couple of pending CWG issues have been resolved; like begin/end for expansion statements. I presume we don’t have to change anything in his PR as a result, but I need to validate for conformance.
  * Vlad: on the C++ reflectors, Jakub has found some issues with define\_static\_array and it might be related to the implementation of expansion statements. We should consider all the still-open and other relevant core issues before landing changes here. Might be a bit hard because Ambrose is not on the committee.
* Nhat’s progress
  * Just created a pr for std::meta::info: [https://github.com/llvm/llvm-project/pull/190356](https://github.com/llvm/llvm-project/pull/190356)
  * Ready for the first round of review.
  * Hubert asked for compiler explorer links, that’s still to be done but will be handled shortly
  * No blockers currently, just initial work on the type.
  * Vlad: at the moment we only have reflexpr over types and nothing more complicated?
  * Nhat: only primitive types currently
  * Vlad: just making sure that tests for this are as complete as can be at the moment, which sounds like it is
* WG21 meeting in Croydon
  * Aaron: let’s keep this high-level instead of deep technical talk unless it relates to what Nhat’s working on right now.
  * Vlad: we have the notion of consteval-only type in the wording, which is meta::info and anything you can derive from meta::info. The problem is that the definition is currently written as-if the compiler has global knowledge of all the TUs in the program. That’s not implementable. There were two approaches to solve this: 1\) fix the definition of consteval only types to split between global and local properties (you can observe local things in your TU and the global one is more like ODR) – this is “consteval types” approach. 2\) “consteval only values”, from Barry and Daveed. When doing constant evaluation, check whether the value you get back has any meta::info within it; if it does, it’s a consteval-only value and we ensure it cannot leak into runtime. Approach (2) was the one the committee had stronger support behind, but the wording will happen in C++29.
  * Shafik: we also discussed implementations. We asked how GCC implements this. The types require keeping a map and later on when the type is completed, you check then. Barry found the values approach to be just another flag in the constant expression engine rather than requiring a full map of types.
  * Vlad: just to implement consteval only types, we’d have to maintain a state machine for every type (two bits should be enough). But for diagnostic quality, we would need to keep a side table of source locations so that we could point the user to the code paths involved. I am not convinced this approach is going to be sufficient; it seems so, but I’m not convinced.
  * Vlad: there were some other fixes to things like allowing annotations on function parameters, constification of splice expressions (which relates to contracts), assortment of fixes for reflection from Barry ([https://wg21.link/P3795](https://wg21.link/P3795))
  * Shafik: have you been talking to Dan?
  * Vlad: on a daily basis, but on a particular topic?
  * Shafik: I ask because we’re going to have Core telecons again. Do you know if Wyatt is abandoning his approach?
  * Vlad: (answer unclear or perhaps not public)
  * Shafik: so Dan is helping both of them?
  * Vlad: Dan helps them write the wording.
  * Shafik: do we have a PR to review? Is something blocking?
  * Vlad: we have Nhat’s PR to review

Meeting Agenda \- March 20th, 2026
(Meeting attendance was light due to the WG21 meetings next week, so this meeting was more informal.)

* WG21 is meeting in Croydon next week
* [P3816R2](http://wg21.link/P3816R2) is going to be discussed and Valentyn is present in our meeting, so we discussed this paper.
  * Vlad: “two instances of H” is confusing, so is that a reflection of the type?
  * Valentyn: H is reflection of the type, yes
  * Vlad: if so, then H is an entity; maybe you want to say “two declarations of H” but maybe not because you’re going for the entity?
  * Valentyn: just trying to see if the idea is sensible at EWG, definitely not ready for wording level review; still needs to go to SG7 and other folks too.
  * Vlad: ideally, the easiest implementation of this would use a pointer to the canonical AST node, but that’s not going to be stable across compiler runs.
  * Valentyn: that was the first implementation we did but it was not stable. Current approach is sensitive to changes in source code, like declaration order but stable to repeated runs without changes to code or order
  * Vlad: I think you’d need a good justification for why we need to go to that level of specification difficulty with “across repeated compiler runs”.
  * Valentyn: the stability comes from a discussion with Hana; want to avoid unreproducible builds
  * Vlad: that is somewhat motivating, but those values are still going to be different across TUs.
  * Aaron: yeah, reproducible builds are important to users
  * Aaron: we give users knobs to control non-deterministic features for things like \_\_TIME\_\_ and \_\_DATE\_\_, so we could avoid the requirement in the standard and leave it to QoI (which avoids the overhead for everyone not using this functionality)
  * Vlad: yeah, we don’t want users to pay for features they’re not using, even for compile time features.
  * Valentyn: these are good points, I’ll update the paper with some more details
  * Vlad: current design for opt-in in the paper may be problematic; we’d have to identify standard library facilities and retroactively figure out which entities we need to make a stable id for. It kind of works, but requires you to squint. We’re basically angling for something the user opts into with a flag
  *

* [CWG3150](https://cplusplus.github.io/CWG/issues/3150.html)
  * Vlad: This is about consteval-only, currently specified as a global static property of the type. The idea is to not let info leak into runtime. When the evaluator gets a value, check the value for meta::info to see if there’s a potential leak to runtime.
  * Aaron: are nested constevals an issue, do you have to walk up the stack of them?
  * Vlad: I don’t think so, it’s on a single level
  * Ambrose: static constexpr variable whose initializer contains a meta::info. That would be valid inside a consteval function but wouldn’t be valid outside a consteval function right?
  * Vlad: allowed today, I believe
  * Ambrose: we have to take care that we don’t actually emit that variable
  * Aaron: is this function type observable consteval-only? void func(int ptr\[sizeof(std::meta::info)\]);
  * Vlad: wording will not survive core I suspect
*
* Various papers
* Nhat’s progress
*

Meeting Agenda \- March 6th, 2026

* Scheduling in the presence of DST timezones
  * Aaron: DST is on in the US starting this Sunday
  * Hubert: next meeting is perilously close to WG21 meeting
  * Corentin: I won’t be able to attend
  * Aaron: next meeting april 3rd?
  * Aaron: I’ll host the next meeting, and we’ll see what the attendance is
  * Vlad: we picked this time slot based on a narrow window of availability; so are people still able to meet at this time?
  * Corentin: the meeting in April will be back on the same times for everyone, so it’s only an issue for the 20th
  * Vlad: meeting is scheduled in UTC time
  * Hubert: not on the calendar
  *
* Nhat’s progress
  * Nhat: I’m working on meta::info.
  * Corentin: next week there is C committee meeting, then C++ meeting, so reviews will be slow anyway
  * Aaron: people will start digging out in april
* [https://github.com/cplusplus/nbballot/issues/739](https://github.com/cplusplus/nbballot/issues/739)
  * Corentin: I wasn’t there when LEWG looked at it
  * Aaron: it wasn’t accepted, so there’s nothing to do?
  * Corentin: but I don’t know how to implement reflection portably
  * Corentin: we’ll be the most affected, because other implementations care less about other standard libraries
  * Ambrose: you can allocate an array with meta::info
  * Corentin: we’ll have to do everything twice: ask for size, then allocate
  * Corentin: I tried LEWG to give us a portable interface
  * Ambrose: can’t we call to resize?
  * Corentin: making calls from frontend is awkward for us
  * Corentin: library has to create that vector
  * Aaron: we need library to construct the vector, then pass it to the compiler. But we still need the size
  * Aaron: how do we proceed?
  * Vlad: can we still coordinate with libraries?
  * Corentin: AFAIK, GCC decided to intercept every call to std::meta and implement it in the compiler
  * Jakub: we intercept the calls. We lookup std::vector in the library when we need to return it
  * Marek: some of the functions are implemented in the standard library, though
  * Aaron: we let users pick the standard library, so we try to not look into the library
  * Hubert: so with the GCC case, builtins are not used?
  * Marek: yes
  * Jakub: one exception: is\_string\_literal
  * Hubert: that tells us about the metafunctions, and how user calls them
  * Hubert: Corentin’s concern was how to return std::vector
  * Corentin: Jakub said they are looking up the vector constructor and call it
  * Jakub: we have a vector within compiler that we can resize, etc. then create std::vector from this prepared data
  * Hubert: so you’re creating virtual code to call the library
  * Jakub: we create the vector as if user created std::vector\<info\> themselves
  * Hubert: so GCC didn’t do magic in a sense of knowing the layout
  * Corentin: we don’t like to do this, because the API to call library is clunky
  * Ambrose:
  * Aaron: we can, and maybe we’ll have to, but it might become a maintenance burden. We don’t know how to look up through inline namespaces. ABI compat?
  * Corentin: compiler doesn’t know about std::meta either
  * Aaron: so you want to vend us as a part of clang headers?
  * Jakub: if clang does something different, stdlibc++ will have to do another implementation for clang
  * Corentin: I’m afraid if we do everything in the compiler, maintenance will be harder to maintain
  * Aaron: my intuition is that we’ll want to match GCC behavior
  * Corentin: I know that in Dan’s implementation use builtins, but some are pure library. We can end up in a situation when clang and GCC intercept different subset of calls. Compiler takes priority over library definition
  * Aaron: yeah, we also do this for std::move
  * Ambrose: we already have to do this for range-based for loop (ActOnBinOp)
  * Aaron: first we’ll need to improve our facilities, which makes the feature harder
  * Corentin: then we don’t know what MSVC is going to do
  * Vlad: I expect them to support clang for their own needs
  * Ambrose: if we intercept the calls, then what msvc is doing doesn’t matter
  * Hubert: I find it an interesting idea to vend \<meta\> with clang
  * Corentin: the library would include\_next or something
  * Aaron: microsoft’s STL will frequently add debug assertions. How are we going to handle that if we’re intercepting
  * Corentin: people would want to do this, because debugging consteval is terrible
  * Corentin: if microsoft decide to have different compiler behavior…
  * Aaron: \#define before \<meta\> will be lost if we intercept the calls
  * Hubert: are we moving away from needing to ask committee for anything?
  * Corentin: I made an NB comment to make the committee to discuss, but apparently they didn’t discuss how to implement it portably
  * Aaron: they said “tough”. We can go back to the committee and ask again, but I don’t think anything will happen
  * Corentin: yeah, design will not change
  * Vlad: is Jakub or Marek are amenable to builtins?
  * Marek: I don’t want to switch to builtins
  * Corentin:
  * Ambrose: true, but due to nature of consteval we still do checks for UB, so we can put asserts in consteval
  * Marek: good idea
  * Jakub: you can intercept the most complicated metafunctions, like type traits
  * Corentin: what needs to happen is once Nhat implements meta::info, we’ll need one metafunction to be implemented, and see which approach works the best
  * Aaron: agreed
  * Jakub: maybe is\_type is too simple. Better something that returns std::vector\<int\> and takes a range of arguments
  * Corentin: how does GCC takes a range as a span to check constrains?
  * Jakub: if it takes a range, we’ll emulate range-based for, and consteval it immediately
  * Alicja: is clangIR relevant here?
  * Aaron: not really, because it’s about codegen
  * Corentin: I’m looking at the list of functions. Some of them are complicated with ranges and constraints. Complicated for clang
  * Jakub: if it’s a template metafunction that takes a concept, only the definition is on the compiler side, except for couple of functions. We define std::meta::exception in the header, then cases when we need to do magic with encodings
  * Aaron: the action item is to explore the options
  * Corentin: yes, we can’t do anything until we have meta::info
* [P4032R0](https://isocpp.org/files/papers/P4032R0.html) “Strong ordering for meta::info”
  * [https://github.com/itanium-cxx-abi/cxx-abi/issues/208](https://github.com/itanium-cxx-abi/cxx-abi/issues/208)
  * Aaron: CAT recognized that itanium ABI group needs to become active. We can’t ask for an ABI, we need to propose it.
  * Vlad: there was an idea in the language working group that we could dump the address of an AST node when mangling
  * Corentin: I didn’t realize we needed mangling for meta::info in such a way that we’d need the same mangling across TUs but we do (so cannot use pointers)
  * Jakub: you can, yes. The reason I started working on mangling was because the work in Dan’s branch was just only partially correct but needed further work. GCC has it implemented in the way it is in the issue. If there are changes proposed and agreed on that come quickly, we could maybe change it for GCC 16 because it’s still experimental. The longer we wait, the harder that gets. There is still work to be done for splices, GCC currently crashes.
  * Vlad: on to the paper for ordering meta::info; does this cause us issues for strong ordering?
  * Corentin: maybe. It’s a C++29 paper that does two things. 1\) meta::info are strongly ordered, 2\) redefine the ordering of types in terms of the ordering of std::meta::info for consistency. The issue there is that it might be an ABI break depending on which order we implement which paper. I don’t think we’ve implemented type ordering yet. Has GCC?
  * Jakub: we do and we use the Itanium mangling for the type and just compare the strings. We’d likely implement meta::info similarly by ordering the strings
  * Corentin: is the ordering of std::meta::info consistent with the ordering of type?
  * Vlad: if both are in terms of mangled names, yes?
  * Corentin: they might not mangle the same way, I don’t know
  * Jakub: type aliases are different for reflection than types, so that’s a case that can mangle differently and compare differently
  * Corentin: yeah, that’s my concern
  * Jakub: and is\_type is true for type aliases
  * Corentin: if the paper removes the part about type ordering, I think we’re fine with it
  * Vlad: do I understand correctly that the argument is: mangling type alias directly and mangle reflection of type alias, we might get different results?
  * Jakub: I think so?
  * Hubert: how would you end up with the type alias outside of reflection which retains its alias-ness?
  * Corentin: outside of reflection you don’t, but with reflection you do. The paper would use the mangling of the alias.
  * Hubert: but the type ordering interface doesn’t work directly on reflection, so by the time you get anywhere the alias is gone
  * Corentin: maybe I see what you mean
  * Jakub: if you compare two reflections (one alias, one not, but they’re not the same) Type ordering would say they’re the same
  * Vlad: type ordering is used for equality comparison of meta info?
  * Jakub: I think the paper mentioned it, but maybe not in the proposed wording
  * Vlad: I see the wording, “if both operands represent the same entity”, so it seems like type ordering is not involved. I think aliases are a different entity even though they quickly turn into their type.
  * Hubert: yes, they’re distinct entities
  * Vlad: maybe we’d be fine with the paper if we changed mangling to be that of aliases outside of reflection. But maybe we can’t do that. So feedback we’ll give the authors is “think about aliases”.
  * Jakub: if the paper says explicitly “neither is type alias then use type order otherwise use this”, that is implementable
  * Ambrose: wouldn’t the mangling just work out because it includes the type assuming it’s not a type alias?
  * Vlad: Tim is saying type aliases are not subject to ODR.
  * Hubert: what is the linkage we’re going to give (at the codegen level)? We’d have to treat these as magically internal linkage?
  * Jakub: yes, we have a test case for this in GCC
  * Vlad: so we’re saying we’re not going to mangle them because they’ve got internal linkage.
  * Hubert: no for codegen, but yes for ordering; so this is a completely new idea
  * Vlad: maybe we can say “for purposes of type ordering, reflections of type aliases compare equal” or something along those lines
  * Corentin: not being able to compare aliases would also solve the issue

Meeting Agenda \- February 20th, 2026

* Nhat’s progress
  * Nhat: started working on next step, which is meta::info and completing parsing of reflect expressions
  * Hubert: I thought meta::info is the first priority, then metafunctions, before going back to parsing
  * Nhat: we have parsing for primitive types
  * Hubert: I thought we want to go depth-first to develop all the supporting layers
  * Dan: we wanted to focus on smaller changes that focus on meta::info before moving on to adding more parsing support (agreeing with Hubert)
  * Corentin: makes sense
  * Hubert: focus only on primitive types through past at least a few meta functions; after that point we’ll want to know if we want to do anything about splices
* Corentin, Dan, and Hubert [discussed](https://github.com/llvm/llvm-project/pull/173537#issuecomment-3923099875) instantiation during constant evaluation
  * Corentin: before christmas I created a PR for SemaProxy to handle instantiations during constant evaluation. Back then Hubert had reservations. Following that were discussions that I missed, where it was considered that genie is out of the bottle. Core is also going in this direction
  * Shafik: what are consequences?
  * Corentin: consteval and templates will get intertwined, diagnostics will be mess. We should get back to what Ambrose was working on.
  * Ambrose: you’ll have all errors in the row. This is an existing problem ,and I see how it can get worse here
  * Hubert: how are we planning to identify chain of events? GCC would tell that instantiation happened at the point of consteval call
  * Corentin: We will do the same
  * Hubert: I’m on the same page that this is the direction we’re going in. I’ve heard Dan is working on formalization of that. Core did not have too much exposure to it. Dan opened some Core issues, but I haven’t looked at them
  * Dan: they are not related to this topic
  * Corentin: I don’t want to commit, but I can help you write papers
  * Hubert: if you have time, we need to get fold constraints working
  * Dan: Corentin, I’ll ping you
  * Aaron: “genie is out of the bottle” bothers me
  * Corentin: my PR is unrelated to reflection
  * Aaron: we are conforming without the PR
  * Corentin: GCC has been doing this for years
  * Corentin: libc++ is not usable in C++23 because of constexpr unique\_ptr
  * Aaron: yes, C++ committee broke us
  * Hubert: there is a question who broke us: Core or Library. There have been longstanding Core issues in this area. We tried to fix it in Clang using an alternate, but it didn’t work
  * Aaron: we’re broken currently, but if we change, do we break users?
  * Hubert: we are broken currently, but if we change to something other than what GCC does, we’ll also break code.
  * Aaron: I’m not comfortable with the PR
  * Corentin: tell me what I can do to make you happy
  * Aaron: I’m still concerned that this will make things much worse
  * Corentin: \<explaining how things will work if PR is merged\>
  * Hubert: Aaron is worried about abuse of library facilities that we provide and your explanation describes a protocol to ensure that such abuse does not happen?
  * Corentin: the issue with the library is that they only have access to ASTContext, and not Sema
  * Aaron: I’m still not understanding: if I have a ASTContext library, how does this work without linking to Sema
  * Corentin: it can’t
  * Aaron: this is the concern
  * Dan: why can’t it?
  * Corentin: it can because I did not need to change any build files
  * Hubert: you need buildbots to test whether you broke layering
  * Aaron: I think some of this pain is coming back to me
  * Aaron: there’s going to be a default implementation that does nothing
  * Corentin: I removed it, because it doesn’t help. Interface is virtual. If you don’t have sema, you get nullptr. Constant evaluation still need to check whether it does something
  * Aaron: let’s not get too far into technical details here
  * Corentin: the patch has been up since christmas
  * Aaron: this explains why I didn’t see it
  * Aaron: we’ll take the review online
  * Vlad: are we still pursuing CWG2497?
  * Corentin: it’s not clear what the issue is about
  * Vlad: CWG2497 has a note that suggests another point of instantiation after the function definition
  * Corentin: we’ll write a paper for Core for that
  * Corentin: there is also CWG2166
  * Aaron: is the person who write that paper here?
  * Dan: it is a potentially empty subset of me and Corentin
  * Vlad: if we go with Corentin’s patch, we don’t need to fix CWG2497
  * Corentin: we need SemaProxy anyway
  * Corentin: part of my motivation is to get the architecture right
* Gaby [has concerns](https://lists.isocpp.org/ext/2026/02/26931.php) about Core’s resolution for [CWG3123](https://cplusplus.github.io/CWG/issues/3123.html) “Global lookup for begin and end for expansion statements”
  * Vlad: two weeks I was supposed to give EWG a presentation on this issue, but EWG was consumed by contracts. So this will be presented in Croydon. Core issue is about how we select between iterating and destructuring expansion statements. Gaby is concerned that for types which do not intend to support begin and end, those functions would still be instantiated to determine their viability before moving on to destructured expansion statements but you can hit non-SFINAE errors. He has potential deployment concerns. I don’t buy it; if you have begin and end with a non-SFINAEable error, your life is already hard if you try to use a range-based for loop. I’d be advocating for Core’s approach in Evolution. Do we have a different opinion here?
  * Dan: I agree with Vlad’s take; I don’t understand the extent of Gaby’s concerns. I spoke to Cody (MSVC) and it sounds like any changes to instantiation strategies are concerning.
  * Vlad: is this a change?
  * Dan: I don’t know. I know they’re working on immediate escalation and maybe are hitting existing issues around instantiation because it requires a more eager approach than they’re used to.
  * Ambrose: there’s a begin but there’s not a matching end; that is what GCC does, but that’s a bad design
  * Vlad: resolution is correct, but GCC behavior is odd
  * Dan: I think GCC may have merged a fix for this (perhaps yesterday)
  * Corentin: what is GCC’s solution?
  * Vlad: Ville claims they’re doing one thing, but the behavior in Compiler Explorer doesn’t seem to match; I can’t figure out exactly what GCC is doing
  * Dan: confirmed they merged the proposed resolution for this yesterday; I think it was what core was proposing. It could be worth revisiting on CE to see if it makes more sense.
  * Vlad: I can do that
  * Ambrose: it may take a while for CE to update
  * Vlad/Dan: should be done nightly
  * Vlad: what I am hearing is that I should go forward with my plan rather than support Gaby?
  * Room: yes
  * Aaron: agree with room but if Gaby has a more compelling example, I would support him – instantiation changes \*are\* disruptive, so if he has problems deploying that, it’s worth knowing about them.
  *

Meeting Agenda \- February 6th, 2026

* Nhat’s progress
  * [PR](https://github.com/llvm/llvm-project/pull/164692) about reflection operator parsing got merged
  * Going to focus on remaining items for reflect-expression next
  * Hubert: my concern is that we’re doing parsing, and then I’m not sure what we’re generating out of it. We need meta::info, even as an opaque type, to do meaningful testing.
  * Nhat: agree. Implementing meta::info might be the next step
  * Dan: there’re developments on consteval-only types that might influence this
  * Hubert: now compiler doesn’t enforce some restrictions around consteval-only types
  * Dan: you mean trunk?
  * Hubert: yes
  * Hubert: it would alleviate my concerns if we run tests through the other compiler (Dan’t fork)
  * Shafik: Hubert, do you have a suggestion or this is a general concern?
  * Hubert: I think we want to have an informal policy that tests should be tested against Dan’s fork
  * Vlad: makes sense to me. How is it going to work? Locally? Godbolt?
  * Hubert: godbolt links in the review, I guess
  * Shafik: makes sense to me
  * Dan: happy to review any tests
  * Vlad: Dan, is your fork separate from Bloomberg’s?
  * Dan: Bloomberg’s fork is dated, but the best we have
  * Hubert: Dan, do you have an opinion on whether we should ignore EDG? It’s not up-to-date
  * Dan: I agree that we should ignore it
  * Hubert: going back to parsing. Not having meta::info made tests less useful, with weird limitations. There’s still a scoping issue. Going breadth-first leaves us with a lot of danger in the future. Can we go depth-first?
  * Hubert: limit parsing, but take it forward, create meta::info, maybe even down to splices.
  * Dan: I’ve always thought of splicing, expressions, and metafunctions as separate branches of P2996. There’s a separate question of how we’re going to implement it upstream. Tablegen? You might be right that meta::info is the right approach. Maybe omitting consteval-only semantics, since it’s still in flux. What do you think, Hubert?
  * Hubert: makes sense
  * Dan: or implement the tracking of consteval-only, but not the semantic restrictions
  * Hubert: maybe we want to avoid the consteval-only question for now
  * Hubert: if we have meta::info, we can have metafunctions. Let’s try to get one query in
  * Vlad: what about instantiation problem that we want to discuss in Core?
  * Dan: we can implement scaffolding, then implement metafunctions that do not implicitly instantiate
  * Hubert: the ship has sailed on whether you need to instantiate during constant evaluation
  * Dan: agreed
  * Hubert: in which instances do synthesized point appear, going back to the issue of point of instantiation
  * Dan: we could have a call to members\_of, which needs an implicit instantiation. The question is that compilers like yours what to constant-fold everything. We want to specify that point of instantiation is right after the expression. But then we have tentative evaluations
  * Hubert: the are treated as manifestly constant-evaluated
  * Dan: but we can’t treat them this way
  * Hubert: I don’t agree
  * Dan: if we were to pursue this, which I think we should, while you determining
  * Hubert: it’s time travel, but not time travel
  * Dan: it’s about evaluations you hit before you give up
  * Hubert: yes, but I was trying to explain what it was not
  * Hubert: compile is a process during which we have evaluations, but the standard doesn’t describe it this way: it will state facts, like whether something is a core constant expression. It’s something that compiler determines after following a process. If side effects happened, this is bad. WE’re trying to say that there’s a portion of evaluation that is going to happen anyway. The other prong of the discussion is where the point of instantiation is. We’re trying to tie to where the evaluation emanates.
  * Shafik: it almost feels like we need a design paper
  * Dan: probably
  * Shafil: every time we discuss this, I feel like I’m diving into a cold water. This needs to be laid down on a paper.
  * Hubert: sometimes we’re writing papers to resolve core issue, not always going through evolution
  * Shafik: sure
  * Hubert: I thought you want a trade-off review
  * Shafik: no, trade-off within core
  * Dan: I’d already written it if I had time
  * Shafik: Hubert, are we going to discuss this in core?
  * Hubert: it is one of those places where you need a paper before discussion in core can happen
  * Dan: I won’t have time to put a paper before Croydon. I have ideas which someone else can put into a paper. This is one of the places where we can strengthen the specification
  * Shafik: what is it blocking? Or we can go forward assuming it’s going to be resolved in a particular way
  * Hubert: it’s safe to assume that our ideas to resolve this will be accepted, because it feels natural to me. Dan, when one of the metafunctions get to a point where it needs instantiation, we stop?
  * Dan: yes, we do it in many places
  * Hubert: compilers arelady separately establish the idea where the point of instantiation comes from. Dan, you have a wording, but we don’t have a Core issue that Jens can open
  * Hubert: in the end, I think this issue is to reflect in the wording what the implementation reality is. Fixing the wording gives us a better idea how things work, but it doesn’t block us
  * Dan: status quo is not worse than the rest of the template wording
  * Shafik: sure, makes sense
  * Hubert: I think we haven’t confirmed how we’re going to proceed. We discussed a lot of things, now need to decide. Does Nhat understand how to do meta::info.
  * Nhat: we’re doing meta::Info, but without consteval-only
  * Hubert: we don’t need to do anything on codegen for meta::info
  * Nhat: works for me, I’ll follow-up when I start working
* Dan: [“Constification fails to apply to splice-expressions”](https://github.com/cplusplus/CWG/issues/844)
  * Dan: I was reading about contracts and ran into this. Constification says that if you name outside variables, they are const. Reflection and contracts went in parallel through the committee. If you designate a variable through a splice, constification doesn’t happen. Compilers should be able to diagnose the example. We need to integrate splicing into constificatin wording
  * Vlad: not a fan of constification, so this might be a feature, but still worth bringing up for consistency.
  * Dan: me neither
  * Hubert: makes sense to me
  * Aaron: is this applicable when the variable is not named directly?
  * Dan: yes
  * Vlad: constification would apply to the result of splice-expreesion, irrespecituve of what’s within
  * Aaron: makes sense, thank you
* Dan: pointers to consteval-only types
  * Dan: let’s open CWG3150
  * Dan: I hate this issues
  * Dan: we look at consteval-only as being a global property of a type, but from a given point you don’t know whether a type is consteval. Especially fun if you go cross-TU. My current thinking is that GCC does a good thing. They don’t care and don’t emit anything that is consteavl-only. You get a linker error.
  * Hubert:
  * Dan: my goal is to never force an implementation to emit meta::info. Maybe it’s fine to emit composite types with it, as long as you don’t need to emit info itself. Something like function with info\* parameter.
  * Hubert: so this is why you’re saying IFNDR
  * Hubert: this direction leads to question of what is the value of having consteval-only types in the specification.
  * Corentin: if nothing in your type is odr-used, it won’t be emitted anyway
  * Hubert: one of the more recent core issues opened was that there is a way to make compiler emit it, which caused Clang ICE
  * Dan: I think that crux motivation to have consteval-only types was that by prevening emitting info you ‘re more strict on zero-cost nature of reflection.
  * Hubert: this pointer concern doens’t hold. You have a ponter to meta::Info. If it’s null, you’re good.
  * Dan: if pointer type is not consteval-only, why can’t you use it
  * Hubert: hmm, you might be right
  * Corentin: we’re forcing frontend to do a lot of work to prevent emitting an integer that is meaningless. I don’t think we need to do anything
  * Dan: if you can have infos at runtime, people would try to have hashsets of them at runtime
  * Dan: I wouldn’t underestimate reflection being zero-cost
  * Hubert: we made this mistake with inline variables
  * Hubert: but with info it’s IFNDR, so you’re just allowing it to be zero-cost
  * Dan: standard can’t guarantee zero-cost

Meeting Agenda \- January 23rd, 2026

* Nhat’s progress
  * Nhat: Erich approved the PR, Aaron and Corentin left some comments. Continue to work on this after this meeting
* (added after the meeting) Aurelien (contributor from BB) is waiting for next iteration of review for "C++ Annotation" PR \-- [https://github.com/llvm/llvm-project/pull/166287](https://github.com/llvm/llvm-project/pull/166287)
* Hubert: follow-up on discussion about new point of instantiation at the end of template definition
  * [CWG2497](https://cplusplus.github.io/CWG/issues/2497.html) might be the right vehicle for WG21 discussion on this
  * Also see [https://github.com/llvm/llvm-project/issues/73232](https://github.com/llvm/llvm-project/issues/73232)
  * Hubert: Jens did not point be to this core issue (2794). Telecons don’t have enough quorum to discuss this, so waiting for in-person meeting. A paper might be needed
  * Shafik: agreed, this will need a significant effort to get right
  * Dan: Daveed mentioned that they instantiate during evaluation, and that MSVC does something similar
  * **Hubert will email Jens to ask him to discuss 2497 CCing Corentin**
  * Hubert: (speaking about 2497\) there is a fine line understanding that g is used in a manifestly-constant evaluation, so that there’s a dependency on the definition of f\<int\>()
  * Dan: we need to do dynamic path instantiation for reflection
  * Hubert: we need an example, and see what other implementations do
  * Dan: the problem is that it can be arbitrarily nested in function calls
  * Hubert: there has to be a structure that describe the dependencies
  * Hubert: then we’d need to know whether side effects of define aggregate was observed
  * Dan: define\_aggregate can’t define arbitrary structs. You have the same issue with friend injection
  * Hubert: I have an idea what two models look like, but I have hard time coming up with examples to show what differences are
  * Dan: define\_aggregate makes it observable whether you instantiated something
  * Dan: if we’re going after instantiation during evaluation model, at least we can try to specify precisely when instantiations are produced. This is made harder by immediately-escalated expressions. Tentative evaluations make it harder, too. Instantiations would persists even after failed trial evaluations
  * Hubert: if we’re going to codify all the stuff out of constant-evaluation, we could also codify odr-uses
  * Hubert: there are two cases for instantiation to happen: when you referenced it: you need to for constant evaluation, and needed for codegen. We can codify the latter, too
  * Hubert: g() is code-gened, it references f\<int\>, so it has to be instantiated
  * Dan: f\<int\> already has a point of instantiation at the end of the TU. We can try to say that if the primary template is not reachable from namespace
  * Hubert: you might not be able to parse if the end of the TU is the only point of instantiation
  * Dan: it’s possible to contrive a program that is IFNDR but it’s not possible to translate the program the bad way
  * Hubert: this is why I said there are two cases for odr-use
  * Hubert: IFNDR supports AST-based implementations
  * Dan: sure there’s a well-formed program that contains a template. At least one.
  * Dan: so it was a bridge too far to discard points of instantiation at the end of the TU. we need to see what other implementations are doing
  * Hubert: that is our homework. Then we’ll have enough material to come to Jens and describe what the options are
* Which synthesized points are included in the evaluation context?
  * [https://godbolt.org/z/9M1fhq51n](https://godbolt.org/z/9M1fhq51n)
  * Dan: program points historically describes state of the compiler, despite being a spatial property. Define\_aggregate breaks that, because state of the compiler changes during evaluation. So we introduced evaluation context which includes program points and synthesized points which describes what is observable for translation: both lexical and injected declarations. It works like this: synthesized point is included in the evaluation context, and that point brings corresponding injected declaration into consideration.
  * Shafik: how does this affect instantiation?
  * Dan: evaluation and instantiation interleaved in two ways: evaluation can cause instantiation, and template specialization can have manifestly-constant evaluated expressions.
  * Dan: members\_of can cause implicit instantiations.
  * Dan: [https://godbolt.org/z/o4G4dTPTE](https://godbolt.org/z/o4G4dTPTE)
  * Dan: consteval block is immediately-invoked lambda which is allowed to produce injected declarations
  * Dan: after line 15 evaluation context contains synthesized points corresponding to the definition of S
  * Dan: [https://godbolt.org/z/8oGMx45r3](https://godbolt.org/z/8oGMx45r3)
  * Dan: now we have nested constant expression
  * Dan: sizeof(T) needs synthesized point from define\_aggregate
  * Dan: getting synthesized points into instantiation context makes line 10 work
  * Dan: [https://godbolt.org/z/GhYhszoj9](https://godbolt.org/z/GhYhszoj9)
  * Dan: now on line 8 we have new evaluation context, which again needs the synthesized point.
  * Dan: wording for 73232 can definitely be improved, how and where side-effects happen
  * Vlad: not for the lack of trying — we did try to specify how evaluations and instantiations, but it didn’t bear fruit
  * Dan: it was particularly hard around point of instantiation
  * Dan: synthesized point model is weird, but seems coherent so far
  * Vlad: [https://godbolt.org/z/9M1fhq51n](https://godbolt.org/z/9M1fhq51n)
  * Dan: yes, I want to specify that instantiation context is immutable
  * Dan: the issues here is that line 5 has an initializer
  * Dan: is\_complete\_type has been phrased in terms of evaluation context of the expression under evaluation
  * Dan: but there’s no distinction between runtime and compile-time evaluation in the abstract machine. There’s only one evaluation
  * Dan: the wording asks b on line 5 to be different depending on whether line 11 has executed.
  * Shafik: we need examples that show how the code executes
  * Dan: the question is whether initializer on line 5 has to be cached
  * Dan: in standardese cached is whether initializer is evaluated with the same evaluation context
  * Dan: I’m arguing that caching is what we want. Compilers are already has to verify whether an initializer is constant, and we don’t want them to throw out the result
  * Dan: to fix this I want to tweak the definition of evaluation context
  * Vlad: you can take 20 words and make sentences out of them
  * Dan: b on line 5 is different variable every time
  * Dan: [https://godbolt.org/z/Gr71fjEf7](https://godbolt.org/z/Gr71fjEf7)
  * Dan: I think we want to mandate Clang behavior
  * Vlad: but clang re-evaluates
  * Dan: then I want you guys to change
  * Dan: [https://godbolt.org/z/nfEc4xevT](https://godbolt.org/z/nfEc4xevT)
  * Dan: you cache statics, which makes some sense
  * \===========================
  * Dan: [https://godbolt.org/z/z43PcrsxY](https://godbolt.org/z/z43PcrsxY)
  * Dan: a pair of incomplete classes S and T
  * Dan: they will be completed in the course of execution consteval block
  * Dan: we have function f, which has variable b with automatic storage duration. It observes completeness of S. the observation influences the width of a bitfield in T
  * Dan: in the course of doing P2996 there was an unjustified assumptions that constant evaluations are self-contained
  * Dan: evaluation context is codifying translation state that will be observed by metafunctions
  * Dan: initializer of b is manifestly constant-evaluated
  * Dan: when we were considering expressions under evaluation in consteval block, we’re passing through initialization of b, which is now supposed to observe the side-effects of line 11\.
  * Dan: there is divergence between Clang and GCC, but only for automatic storage duration
  * Dan: b is a new variable every time f is evaluated
  * Dan: formally b is initialized every time f is called
  * Dan: but initializer of b is evaluated for validation purposes, and then we throw away the result
  * Dan: the idea is to specify caching in the wording
  * Hubert: we’ll take it as a core issue
  * Hubert: the expression under evaluation is the smallest one is the model we have
  * Dan: I want implementation feedback on how possible it is to mandate caching
  * Hubert: with respect to the state of constant evaluator, can we create a separate context?
  * Dan: re-entracy is handled quite good. Everything is kept in EvalInfo
  * Hubert: it needs something like this anyway
  * Hubert: the problem you talk about might exist in the rules about starting the lifetime
  * Hubert: it’s a question whether something is core constant expression. You anchor it to the smallest one.
  * Dan: I want to say that initializer context of initializer of b is evaluated in the same context irrespective of how it was reached
  * Hubert: makes sense
  * Hubert: caching is not the question. The question is whether you can create new evaluation context
  * Dan: in clang you wouldn’t re-enter the evaluation context, you return cached APValue
  * Hubert: …
  * Hubert: in terms of how to approach this in the committee, for the sake of transparency, this might be considered an oversight. Example might complicate things. We might process this without looking at code
  * Shafik: context is always important, so we should be explicit more often
  * Hubert: the issue with making every constexpr static is that you have the pure value problem again. We need objects
  * Dan: evaluation of a metafunction is a function of expression itself and of the evaluation context. For other things it’s just the expression itself. If we say that for manifestly contant-evaluations evaluation context is fixed, then it removes the parametrization for them.
  * Hubert: I’m happy with this
  *
* [CWG3150](https://cplusplus.github.io/CWG/issues/3150.html): Incomplete consteval-only class types ([discussion](https://github.com/cplusplus/CWG/issues/836))
  * Dan: Consteval-only types are compounded from meta::info
  * Dan: References and pointers can also be consteval-only
  * Dan: You can have a pointer to incomplete type that becomes consteval-only later:
  * Dan: idea is to make this property dependent on a program point
  * Vlad: like completeness of a type
  * Dan: the initial idea was to prevent meta::info from being emitted
  * Dan: do we really need to disallow emitting meta::info?
  * Hubert: we need to understand the mechanics. Maybe we can check consteval-onlyness on the usage
  * Dan: you can ODR-use B
  * Hubert: but meta::info is something you can’t talk about still
  * Hubert: the really interesting thing is entanglement between consteval-only and immediate escalation and all that
  * Dan: why so?
  * Hubert: you could have a different interpretation whether something is immediate-escalating, depending on where you are evaluating your expression
  * Dan: it would. You have to evaluate immediate invocation after parsing to see whether it’s constant
  * Hubert: but can you have a case when you don’t know if it was an immediate expression
  * Dan: definition of immediate expression will have to be rewritten
  * Hubert: the model will need to change on how we identify the boundaries of constant evaluation
  * Hubert: I don’t know how we deal with something at parse time is immediate or not
  * Dan: I think it’s a matter of type of an expression after you parse it
  * Dan: if you know that something is consteval-only, it’s not changing
  * Dan: we’d run into trouble specifying the second half of \[baisc.types.general\]/12. It doesn’t make sense to talk about object w.r.t. a point. We’d need to better account for temporaries

Meeting Agenda \- January 9th, 2026

* Nhat
  * Nhat: waiting for reviews on the [PR](https://github.com/llvm/llvm-project/pull/164692)
* Corentin is taking over [“Sema in constant evaluator” PR](https://github.com/llvm/llvm-project/pull/115168) as [173537](https://github.com/llvm/llvm-project/pull/173537) (second attempt)
  * Background: some of the generative parts of reflection, as well as things like substitute(), etc require the ability to invoke Sema during constant evaluation
  * Vlad started a PR with a few approaches to try to see how it works. Corentin needs the functionality for something.
  * Corentin: two different concerns. We have needs to access Sema during constant expression evaluation, even outside of reflection. We have some issues already filed on this. But then we need the same functionality for expansion statements as well as reflection.
  * Not trying to answer the question for reflection with this PR.
  * We need a way to tell when constant evaluation can or cannot modify the AST, that’s currently handled by consteval blocks.
  * In some cases, we evaluate a constexpr function before it’s defined and at that point we need to define it.
  * Hubert: that was discussed on the CWG reflectors in Dec. We want to extend the evaluation points for reflection & access context to constant evaluation in general. A lot of it comes down to “usable in constant expressions”. For the variable side, that’s the concept in the wording. That’s depending on the point of the start of the evaluation. But we use the term various times in the standard without specifying the context. The wording is completely wrong because the context is required. Dan and Hubert have been trying to fix that wording on the reflector.
  * Corentin: I’m trying to match GCC rather than the wording in the standard. libstdc++ doesn’t work unless we fix some of these bugs.
  * Hubert: could try to take a look at the wording for mismatches so we can work with the committee to repair it. The committee might still say “no” because the wording is moving towards a principled design.
  * Corentin: we want to instantiate a function in a consteval context unless it is \*not\* a trial evaluation. You don’t want side effects just because you’re trying something. That’s why I’m following GCC’s behavior, the standard doesn’t seem to say anything about this.
  * Vlad: there might be a similar case when we handle most vexing parse where we try to parse initializers and instantiate stuff there and back off when we find that doesn’t work. I’ll dig up an example.
  * Corentin: when doing constant evaluation, if you call a function that’s not been instantiated, you do not want to instantiate it at that point if it’s a trial evaluation.
  * Hubert: the wording says you do not do it in the constant evaluator at all. It’s not a property of the evaluation. It’s a property of whether it’s manifestly constant evaluated. At which point that may \*cause\* an instantiation. You can do the instantiation ahead of time and not avoid the instantiation.
  * Corentin: the issue we ran into is that it’s very hard to do ahead of time.
  * Hubert: but we’re parsing? After you parse, you know what context you’re in, so now you know that all the names within the expression that you have to care about
  * Corentin: trying to avoid walking multiple times
  * Hubert: but that’s basically what the standard requires
  * Corentin: this is a long-standing issue with uninstantiated functions
  * Vlad: relevant examples: [https://github.com/llvm/llvm-project/issues/73232](https://github.com/llvm/llvm-project/issues/73232)
  * Hubert: because we have issues with instantiations as well as lookup context for names, so I’m concerned about trying to do this in the constant evaluator. That moves us away from the model in the standard. I’m fairly certain the committee is not going to be able to review any wording tying instantiation behavior based on the evaluation.
  * Corentin: the standard already says “when needed”
  * Hubert: yes, but “when needed” is based on context. If you have a false path during constant evaluation, it’s not supposed to make a different.
  * Corentin: I don’t think that’s what implementations are doing in practice. Any time you have a constexpr function calling another, you need the instantiation.
  * Hubert: okay, that’s not a constant evaluated context.
  * Corentin: so the standard saying “when needed” is not sufficiently clear.
  * Hubert: the standard has two ways of causing the instantiation. ODR-use and needed for constant evaluation. In the case of the function body, that’s an ODR use. So I guess it’s a question of the order of instantiation that you’re trying to do. Are you trying to say there’s a circular dependency if you try to instantiate before?
  * Corentin: yes, see the link to [https://github.com/llvm/llvm-project/issues/73232](https://github.com/llvm/llvm-project/issues/73232) that showcases.
  * Shafik: any example in particular?
  * Corentin: all of them are isomorphic. In all the cases you call a constexpr function which is later defined.
  * Hubert: what’s the simplest one to look at as an example?
  * Corentin: [https://github.com/llvm/llvm-project/issues/73232\#issuecomment-1824410365](https://github.com/llvm/llvm-project/issues/73232#issuecomment-1824410365)
  * Hubert: that’s an ODR use in the definition, but you cannot do the instantiation at that point.
  * Corentin: Clang is going to try to instantiate that function at the end of the TU unless we do something. But then we use it before the end of the TU.
  * Hubert: and it is defined at the point where you do the constant evaluation. I think if you didn’t defer instantiation until codegen, and instead tracked that you need the instantiation when you see it. That might be a road of madness leading to circular dependency where there’s no good order.
  * Corentin: yes
  * Hubert: the standard wording may not be great; it doesn’t talk about timing of instantiations. The direction we’re trying to move to, at the point you do the call to f() is the point where we care if g() is instantiated or not. g() is needed for constant evaluation and it will be instantiated, but the standard never says when. So the case where you need to instantiate is the case the standard doesn’t say much about.
  * Vlad: the idea to instantiate at the end of template definition is what Richard suggested and Corentin implemented (in the comments on the issue). It had to be reverted. The [next example](https://github.com/llvm/llvm-project/issues/73232#issuecomment-1837139294) in the thread shows the kind of code that doesn’t work, such as things like specializations.
  * Hubert: at some point you have to say that’s not going to work.
  * Corentin: but code relies on this (existing GCC behavior); I don’t want to break Qt
  * Hubert: needs to be discussed in the committee for sure. It’s not like this is the first time where patches had to be made to other projects.
  * Vlad: I think Richard is convinced that code should work ([comment](https://github.com/llvm/llvm-project/issues/73232#issuecomment-2042242736)), but that ship might have sailed. So far Clang seems to be the only implementation upholding the model Hubert described.
  * Hubert: not surprising, a lot of this constexpr wording was driven by Richard (and Jens).
  * Corentin: if we did not have reflection, it might be worth it to not mix constant evaluation and sema. But with reflection, we have to do that anyway.
  * Hubert: I’ve already pointed out to the committee these issues around reflection and point of instantiation. If you start going down the reflection path, then when you do the instantiation, you’re going to drag a point of instantiation with you. In the reflection world, that’s what happens. If you move in the direction you’re suggesting, you’re going to have issues figuring out what your point of instantiation is for name lookup, ADL, etc. The additional aspect that is very surprising is that we end up seeing block scope declarations in the point of instantiation now, that never happened before.
  * Vlad: is unique\_ptr in scope for your PR?
  * Corentin: no, going to come after. Clang has issues when trying to instantiate a constexpr function too late and too early.
  * Shafik: it sounds like what we’re saying is that Clang is strictly following the wording but recent changes in the standard are pulling us in a different direction. Is that right?
  * Hubert: Clang currently does something close to the wording, but won’t cover the first case in Corentin’s issue. What the standard says about that case is not very clear but it leans towards saying it should work. So the attempt at making it work in a manner consistent with what the standard might do exposed the further issue with ordering.
  * Corentin: yes, and I suspect there are many variations of ordering issues.
  * Shafik: so we have too much real world code doing it this way but it will significantly break our users.
  * Corentin: like scenarios where unique\_ptr doesn’t work for people.
  * Vlad: worth mentioning it’s not going to break people; unique\_ptr issue is only for C++23 because that’s when unique\_ptr started having a constexpr destructor. We have the data that not many of our users are on C++23 or newer.
  * Corentin: unfortunately, there’s a difference between what Standard says and was users do
  * Aaron: is Qt broken in just C++23 and newer?
  * Corentin: no, since C++11
  * Corentin: [https://godbolt.org/z/Wz5oMzdnP](https://godbolt.org/z/Wz5oMzdnP)
  * Hubert & Aaron: doing it during evaluation is weird code
  * Aaron: comma operator convinces me that this is weird code (joke)
  * Corentin: are we going to be the only implementation which doesn’t instantiate during constant evaluation?
  * Aaron: the only implementation that follows the standard? That happened before
  * Corentin: I’m not sure whether I should be talking to other implementations or with the committee
  * Hubert: CWG is coming up, we can talk there
  * Aaron: we should talk more to the committee
  * Corentin: agreed
  * Aaron: reflection is experimental all over the place and should not influence our decision here
  * Hubert: even queries require implicit instantiations
  * Aaron: I see this being generative in the same way as define\_aggregate
  * Corentin: then the entirety of the reflection is generative
  * Aaron: why can’t we instantiate upon parsing?
  * Corentin: if you do a query that requires class completion, it requires definitions
  * Hubert: even substitute doesn’t cause instantiation
  * Vlad: what meta::info points to?
  * Hubert: instantiation of declaration, not definition. But (I guess there may be cases where) declaration can require other instantiations
  * Vlad: it’s not news that we need to instantiate stuff other than via define\_aggregate. members\_of requires instantiation. No reflection queries are immune to this
  * Corentin: there is a case of expansion statements, which also need instantiations
  * Ambrose: I don’t remember that we need to instantiate during constant evaluation
  * Ambrose: do you mean creating a lambda out of this air? That happens in Sema
  * Corentin: oh, did I invent something?
  * Ambrose: I can’t think of anything
  * Corentin: Maybe I misunderstood something
  * Ambrose: maybe I didn’t do a good job explaining. We need to evaluate the loop to get a number out of it.
  * Hubert: Aaron, can you give me link to the minutes. We need to discuss whether Google Meet is the best place to have this meeting
  * Aaron: Nick asked to post minutes elsewhere, we didn’t do that
  * **Action item for Vlad to post minutes to Discourse**
  * Hubert: Google Meet works the worst for me. Can we use anything else?
  * Aaron: happy to use anything else
  * Vlad: Discord doesn’t work for you
  * Aaron: I’ll make it work
  * Alicja: Google Meet works so much better than Zoom for me
  * Ambrose: I prefer google meet
  * Shafik: …
  * Aaron: sounds like folks prefer meet. Hubert, if meet is untenable, let us know
  * Aaron: I like you all too much to suggest Teams
  * Hubert: I’m glad to hear that
  * Aaron: next meeting will be held in Google Meet on 23rd

Meeting Agenda \- December 5th, 2025
When: December 5th, 17:00 UTC (9 AM PST, 12 PM EST)

* Initial introductions as needed
  * Aaron Ballman
    * Clang maintainer from Intel
    * Interested from design perspective
    * Planning to help with code reviews, but not direct implementation efforts
  * Nhat Ngyuen
    * From Bloomberg
    * Working on upstream implementation
  * Ambrose
    * Mostly frontend stuff, more review than implementation
    * but working on expansion statements too
  * Shafik Yagmour
    * A Clang frontend maintainer from Intel
    * Member of CWG
    * Active on reviews
  * Corentin Jabot
    * From Bloomberg
    * Don’t plan to work much on reflection
    * Will try to work on exposing Sema to constant evaluation
    * Is going to have opinions
  * Michael Levine
    * From compiler group in Bloomberg
    * Project manager
    * Engage in more projects and help people
  * Valentyn Yukhumenko
    * From bloomberg
    * Contributor to open source projects
    * Beginner-level contributor to LLVM and GCC
  * Vlad Serebrennikov
    * Maintainer in Clang, used to contract for Bloomberg, have been interested in Reflection for a long while
    * Helping coordinate between Reflection authors and other clang maintainers
    * Plan to do code reviews, mainly on the test side
    * Corentin is picking up my PR on exposing Sema to constant evaluation
  * Vassil Vassilev
    * Princeton University, researcher
    * Built clang-based tools around reflection
  * Alicja Texler
    * From Bloomberg  compilers working group
    * Still learning, hoping to get involved with implementation work
  * Hubert Tong
    * From IBM compiler group
    * In C++ standards committee for a decade
    * Hoping to do code review
    * Needs buy-in from management to have time
    * Interested in alignment between implementations and the wording
  * Aurelien Cassagnes
    * Bloomberg engineer
    * Interested in annotations
    * Japanese timezone
* Meeting cadence for the next year
  * Aaron: holiday season, not a good idea to meet in december once again
  * Aaron: is every two weeks works?
  * Aaron:  Jan 9th next meeting?
  * Hubert: I’m available. Today we have CWG, 9th would be the same situation
  * Shafik: this might be a feature. Can discuss Core issues right before CWG telecons
  * Aaron: I’m typically good at ending meetings on time
  * Hubert: I’m fine to have it before Core
  * Aaron: next meeting Jan 9th, same time, every two weeks
* Start discussing the high-level plan for how to surface reflection
  * to Clang users
  * to downstream consumers of clang as a library
  * Aaron: feature is expansive, need to plan ahead even though prototype implementation is available. Experimental feature, don’t want to surface as a feature in older language modes or C until more deployment experience. Do we want to put it under a flag? Flag to control FTM? Cc1 flag?
  * Corentin: I like the idea of only cc1 flag, because it will take a while to make reflection useful and usable. Cc1 should be fairly limited in what it hides. Opposed to setting FTM before the feature is ready. Should not expose in older language modes for now.
  * Aaron: reflection is different for older language modes, because they have different language rules (asking questions about implementation details).
  * Corentin: we’re in agreement. We should not backport features that are not ready
  * Aaron: need distinction between reflection and metaprogramming. Reflection is well-known, but metaprogramming is terrifying. WG21 might not understand the ramifications themselves
  * Vassil: generally it’s a good idea to split the const part for tools, the const parts are thread safe in ways that the generative parts are not
  * Vlad: there are so many things that change during the translation; parameters can change names, get default arguments, namespaces can be reopened, etc. We also have substitute() which makes us instantiate templates during constant evaluation. So it might be more challenging to find the subset of reflection that is clear. Core is making more expressions value-dependent than before, so even basic stuff is changing.
  * Corentin: 1\) it’s going to take us a long time to implementa reflection (\>6 months). We should postpone define\_aggregate. From the user perspective, if they need to pass a flag to get define\_aggregate, this is user-hostile, because it’s a core feature. When we get to it, we should accept that define\_aggregate is there to stay.
  * Aaron: I agree on delaying generative bits. I think the biggest use case is enum\_to\_string and type traits. Generative bits are a different kind of feature which needs a flag. Invalidates assumptions of library authors, because users can change stuff. Not sure what the way is, but the use of generative bits should break the build.
  * Vassil: I think that makes sense. Vlad mentioned that things change during translation, but I meant something different. I’m concerned with changes to AST.
  * Aaron: interesting thing to think about is that we currently rely on some things running in debugger. I don’t want expression evaluator to change AST.
  * Corentin: it won’t work and can’t work.
  * Aaron: if you evaluate rvalue it gives you a value you can expect.
  * Corentin:
  * Vassil: what Aaron means is that you can run expression in debugger, and debugger parses them. Modules are reflections for us. We should be cognizant of this perspective
  * Corentin: AST is finished, you can’t change it
  * Vlad: lldb generates an AST and runs it
  * Vassil: the metaquestion is that clang is different from gcc, because there is an ecosystem of tools around clang, and we’re discussing how to make reflection work for them. Tools will have access to Sema, and will be changing their ASTs. if you debug clang within lldb, and you dump, then there are a bunch of effects happening on the state of clang. We need “no module load” modes to prevent that
  * Ambrose: I see Sema usage in lldb
  * Aaron: Vassil and I are on the same page, but different from Corentin. When I’m debugging clang, I don’t want dump to change anything.
  * Corentin: why would that happen?
  * Aaron: because we already do this with modules
  * Corenin: they are happening at different times.
  * Shafik: you can debug while you’re compiling
  * Aaron: run expression evaluator before TU is complete
  * Corentin: there are specific points where define\_aggregate is called
  * Aaron: we don’t want clang-tidy to redo effects of consteval blocks.
  * Corentin: I think we’re in agreement. AST you have in the tool is not changing
  * Vassil: this might be true from language perspective, but not from implementation perspective
  * Corentin: I think we don’t agree on how clang works or should work
  * Aaron: any of our internal APIs can be called by any tool at any point in time, and in the past we’ve made assumptions that did not alway hold
  * Hubert: I’m in agreement with you about internal APIs. scenario: someone is using internal API to evaluate a constant expression in a consteval block. Not sure if we can protect ourselves from this evaluation.
  * Aaron: this is what I’m worried about. What if they create an AST node and evaluate it
  * Vassil: I’ve seen this problem in libraries. They do hooks into clang to assert if declarations happen in wrong contexts. We’d need to significantly change AST to detect if someone is changing it from outside. It will help modules, too.
  * Aaron: Corentin mentioned that our tools don’t have access to Sema, but we need to think about downstream tools as well
  * Corentin: reflection can’t do arbitrary mutation at arbitrary points, we might be able to protect just those points
  * Vlad: indirect changes to AST: inject a default argument, and let the program to observe it later
  * Vassil: one of the reasons tools want to use clang is to instantiate templates. Not wise to think that tools are not using Sema.
  * Corentin: if we assume that, then it’s the same if you call instantiate()
  * Aaron: we wanted to turn off generative bits.
  * Corentin: it can do very specific things
  * Aaron: can it declare arbitrary functions?
  * Corentin: no
  * Hubert: currently evaluation request from a tool is harmless, but with generative bits it may be not.
  * Aaron: good way to put it
  * Vassil: code will behave differently depending on what tools does
  * Hubert: we need to find subset of things that are not going to have effect on subsequent translation
  * Vlad: even harmless queries like names of function parameters can impact the values the metafunctions produce
  * Hubert: declarations hidden from reflection
* Start discussing the high-level plan for how to upstream existing reflection fork into Clang
  * Timelines of when people are available, including reviewers
  * Corentin: I plan to work over the holidays. For Clang 22, I think we want everything hidden behind a cc1 flag, and do generative bits later. Are we agreed?
  * The room loves the idea
  * Corentin: we can start landing PRs in the next few weeks?
  * Aaron: sure, subject to usual review rules
  *


Request from Nick: post the meeting minutes as something other than a google doc